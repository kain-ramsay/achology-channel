# RULING: the three dash files are clean, one item still waits

**Filed by Claude Code, Session 117. Date:** 15 September 2026.
**Answers:** the dash debt named in `RULING__Dead_Code_Removed_23_Of_24_One_Correction_Found_S117.md`, on Kain's word to go ahead.

---

## What was done

**All pre-existing em and en dashes removed from code comments in `cards.css`, `faq-icons.php` and `knowledge-hub-parts.php`.** 96 lines changed across the three files, every change a punctuation swap inside a comment (dash to colon, semicolon or comma, or "to" for a number range). No wording changed beyond the punctuation, no logic touched. Theme version 0.428.0, deployed and confirmed against the server: every PHP file still parses, and the server, the local copy and the rebuilt zip all agree.

`faq-icons.php` and `cards.css` now carry zero em or en dashes. `knowledge-hub-parts.php` carries four, and they are left in place on purpose.

## The one item that still waits

**Four lines in `knowledge-hub-parts.php` (98 to 101) are not code comments.** They are the cross-category listing page intros for article, book note, quote and workbook, marked in the file itself as verbatim from a signed DSRD 2 section 4.3, and each one carries a dash. Removing a dash from published, reader-facing copy is still an edit to that copy, and Code does not draft or edit published content; that routes through Chat. Left untouched, named here so the debt is not forgotten a second time.

---

OWED BACK: a decision on the four verbatim lines, whichever way Chat and Kain want it: reworded to drop the dash and re-signed, or left as a named exception to the dash rule since they predate it.

*No em or en dashes in this file; checked before writing.*
