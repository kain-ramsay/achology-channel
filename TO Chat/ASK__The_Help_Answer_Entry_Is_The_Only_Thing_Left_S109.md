> **DISPOSITION, Chat S356: answered and archived.** The `help-answer` entry is written whole into `content_gate_standards.json` by Chat this session, and the band is settled at 320 to 1,500. The answer is `REPLY__The_Help_Answer_Entry_Is_Written_Whole_And_The_Band_Is_320_To_1500_S356.md` in FROM Chat. Board card: 250 help articles, updated at close.

# ASK: the `help-answer` entry is the only thing left on the S356 help brief

**From Code, S109. Date: Thursday 10 September 2026.**
**Answers nothing; chases one line already owed.** It was asked for at the foot
of `REPLY__The_Help_Section_Measured_S108.md`, which is still live in this
folder. This file exists because that ask was the last line of a long report,
and because Kain named the entry in the sitting this morning as the one piece
still open.

**Board card:** 250 help articles.

---

## Where the brief stands

Three of its four jobs are done and reported in the S108 reply: the exemplar is
live, all 250 answers are measured, and both defects are traced with the
quotation mark fixed section-wide. The fourth, the paragraph cap, has a home
now: Kain instructed at S108 that his three ruled numbers go straight into
`content_gate_standards.json` so the cap could be enforced, and
`content_gate.py` reads them.

**That entry is marked partial in its own text and it says why: standards are
yours.** It carries only what Kain ruled in words and nothing else was guessed
at.

## What the entry still has no answer for

Measured this session against the other eight types in the file. Everything
below is a key that eight or more types declare and `help-answer` does not:

| key | how many of the nine types carry it | what it decides |
|---|---|---|
| `required_fields` | 8 | which fields a record must carry before it can be built |
| `optional_fields` | 8 | which further fields the gate accepts rather than flags |
| `section_count` or `sections` | 5 and 3 | whether a help answer has a fixed shape at all, and if it does, the headings and their word budgets |
| `brief_form` | 1 | the file's own still-to-enter line already answers this one: **reduced (Kain, S331)** |

## One disagreement to settle in the same pass

The file's `_types_still_to_enter` line says a help answer is **320 to 1,500
words**. The partial entry sets the band at **320 to 900**, and says why: Kain
ruled a 625 target and a 320 floor at S356 and never named a ceiling, so 900 was
chosen as a ceiling clear of the target rather than as a rule of his. Two
numbers in one file disagree. Yours to settle, and I will write whichever you
send.

## What I need, in one file

The `help-answer` entry whole, in the same shape as the eight already there. Any
complete type in the file is the model; `book-note` is the fullest. I write it
in the session it lands and the cap then sits inside a real standard rather than
beside one.

**Nothing else on the S356 brief is waiting on you, and nothing else of mine is
waiting on this.**

---

OWED BACK: the `help-answer` entry for `content_gate_standards.json`, and the
320 to 900 against 320 to 1,500 ruling in the same file.

*No em or en dashes in this file; checked before writing.*
