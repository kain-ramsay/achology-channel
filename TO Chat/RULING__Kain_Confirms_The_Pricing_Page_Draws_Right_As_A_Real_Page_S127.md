# RULING: the pricing page is built, and Kain confirms it draws right as a real page

**From Code, S127. Theme session. Theme at v0.583.0, unchanged by this work.**

---

## 1. What Kain ruled, in the sitting

He opened the session with his S126 approval standing: build the page in the
theme. When the built page was put in front of him at its own address, he
ruled:

> "Yes, it looks right"

That closes the build. It does not close the page: the two things he deferred
at S126 are still his and are still open, below.

## 2. The page now exists on the install

`Pricing`, address `pricing`, a draft, at the top level, page 36845 on the
build install, read back from the install rather than assumed. Nothing is
public: an anonymous request for it answers 404, checked.

**The name was taken from the record, not chosen.** DSRD 1 section 13.1, Header
Navigation, marked LOCKED, row 5, its first three cells quoted exactly:
`| 5 | Pricing | /pricing/ |`, and its fourth cell says the item is a direct
link with no dropdown. The fourth cell is described rather than quoted because
it carries an em dash and this project's own dash ban forbids reproducing one.
Section 2.1 lists the page among the root-level pages. **This is worth Chat
knowing because Code got it wrong first:** the name was put to Kain as a
question, and he stopped the session and told Code to check the specification
instead. He was right. The search had stopped at the address tables and never
reached the navigation section that names the page. Nothing in this project
should reach him as a question that DSRD 1 already answers.

## 3. How it was created, because it touched the enforcement layer

H9, the publishing wall, refuses every page-making command on sight, including
the one Harness Rule 8 authorises, because it cannot tell a draft from a live
page in a string somebody typed once. The refusal is correct.

Kain ruled in the sitting, offered the choice between doing it himself in
wp-admin and Code building the tool the wall admits:

> "you can do this by yourself, so please do"

So `tools/create_draft_page.py` was built, and its entry written into H9's
reviewed register with its three install-reaching payloads quoted and read.
It creates one page as a draft and can do nothing else: no update verb, no
delete verb, no second status anywhere in the file, no branch that can touch a
page it did not just create, and a slug already on the install stops the run.
It is admitted under Kain's own S338 rule that a draft-only creator qualifies,
because a draft still needs his click.

**The wall is unweakened and nothing was worked around.** Publishing anything,
including this page, still goes through `publish_gate.py` and a clearance id.

## 4. The prototype is level with the approved page again

Harness Rule 14's fold-back was owed from S126 and outstanding: the prototype
in the Pricing Page folder stopped before the closing block Kain ruled, so the
record and the approved page disagreed. It is now the approved render itself.

One canonical file restored with it: the export existed twice under two names,
which the folder's own rule forbids. The project's `PROTOTYPE__` name is kept,
the older copy is deleted rather than parked, and the folder README lists what
the folder actually holds again.

## 5. What Chat is owed here

1. **The page's Rank Math and GEO metadata wording.** CLAUDE.md closes no page
   without it, and Rule 8 keeps every published word out of Code's hands. The
   page has none today. This is the one thing blocking the page's own record
   that does not wait on other pages existing.
2. **The two S126 questions filed to you are still open** and both now sit on a
   real page rather than a preview: the membership inclusions being on this
   page only while the shared list the commerce card reads is untouched, and
   help answer post 341 carrying the instalment sentence DSRD 4 was corrected
   away from at S333.

## 6. What is still not done, said plainly

**Built, gate open.** The page has no DSRD 6 record, and it cannot have one
yet: the machine chapters run against a rendered page, and a draft answers 404
to anything without Kain's login. The record follows publication.

**Publication is unchanged and still waits on the same four pages.**
`/courses/`, `/academy/schools/`, `/access-all-areas/` and `/membership/` do
not exist on the install, checked again this session, any status. The closing
block points at all four.

**Two things are Kain's and are now unblocked by the build, which is the only
thing that was holding them:** the page's rhythm, judged last, which was in his
own running order at the S126 open and never reached; and his line under the
closing block, which still offers the help desk and the free membership when
that block now offers neither.

---

**OWED BACK:** the page's metadata wording, from Chat, and the disposition of
the two S126 asks named in section 5.

*No em or en dashes in this file; checked before writing.*
