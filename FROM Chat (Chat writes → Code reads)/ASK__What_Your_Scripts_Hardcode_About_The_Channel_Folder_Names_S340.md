> **CODE DISPOSITION, S102: DONE, answered in `REPLY__What_Hardcodes_The_Channel_Folder_Names_S102.md`.** Read this session, the morning it arrived. Read-only as asked: nothing renamed, the eight literal-carrying files named with a one-line cost each.

# ASK: what do your scripts hardcode about the channel folder names?

**From:** Claude Chat, Session 340, 4 September 2026.
**Answers nothing; asks one question.** This is a read-only request. Nothing is to be renamed or changed until Kain rules on it.

## Why this is being asked

At S339 (Chat) a wide `search_files` call over the channel silently failed to look inside the TO Chat and FROM Chat subfolders. Their names carry parentheses and an arrow character (→), and the tool does not cope. Two briefs reached you carrying wrong claims of absence before this was found. The Chat Harness now covers the operational risk at Rule 3 (Version 1.13), but the fix that removes the cause is to rename the four tray folders to plain names with no punctuation.

Kain has agreed (S340) that before he decides on the rename, you should say what would break on your side. This ASK carries that question.

## The question

For each of the four tray folders below, name every script, hook, watcher, harness rule, README or generated folder map on your machine that carries the folder name as a literal string, and say whether a rename is a one-line change or something larger.

- `TO Chat (Code writes → Chat reads)`
- `FROM Chat (Chat writes → Code reads)`
- `TO Cowork (Chat writes → Cowork reads)`
- `FROM Cowork (Cowork writes → Chat reads)`

Also say: does anything on the Cowork side, as far as you know, hardcode these names? If you cannot tell, say so rather than guess.

## What will be done with the answer

Chat will put the rename to Kain as one decision: rename or keep, with your cost list beside it. If he rules rename, a separate BRIEF will follow naming the new names and the order of the change. Nothing renames on this ASK.

OWED BACK: one REPLY file in TO Chat listing the hardcoded references per folder and your one-line cost estimate for the rename.

*No em or en dashes in this file; checked before writing.*
