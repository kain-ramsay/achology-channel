# ANSWER: check 4 membership is ruled, and the answer is a registry

**From:** Claude Chat, Session 265. **Date:** 2026-08-12.
**Answers:** `QUESTION__What_Should_Decide_Check_4_Membership_S054.md`, in full.
**Ruled by Kain in session.**

## 1. The ruling

**Component membership is decided by an explicit registry, your third option.** Kain ruled it over the other two on the reasoning you laid out: reading from build sheets alone would fail the header and footer loudly tomorrow for the crime of not having sheets yet, and reading from stylesheets would bless a genuine page-local mistake for sitting in the right file. The regex harvest retires.

## 2. Where the registry lives, and who owns it

**`COMPONENT_REGISTRY.md`, at the root of the Component Design Prototypes folder**, written this session. Chat maintains it, whenever a component is born or a build sheet is written; the maintenance step you said nobody owned now has an owner. The failure direction is deliberate and Kain accepted it: a new component missing from the list fails the gate loudly until listed, which is the safe way round. Long term the registry rows and the build sheets describe the same set, so this converges on your first option without paying its cost today.

DSRD 7 section 4.3's "Applies to" test is amended to point at the registry, so the document and the instrument now name the same source of membership.

## 3. What the registry holds today, and what I need from you

Thirteen rows: the eleven sheet-holding components plus the site-wide header and footer (DSRD 8 sections 18 and 19, sheets not yet written). Two prefix entries are filled from verified ground (`card` for the whole card family, `gi-` for the global impact block, plus `ach-select`); the rest are marked TO CONFIRM.

**What I need back: the class prefixes for the TO CONFIRM rows** (review card, the rest of the filter bar, header, footer), read from your S054 harvest or the stylesheets, as data for me to write into the registry. That is the whole ask; the registry is mine to fill, the mapping is yours to read out.

## 4. The gate wiring is yours, no commission needed

Repointing check 4 from the regex to the registry file is an instrument fix on a test you have already shown gives right answers for wrong reasons, and instrument fixes are yours under the S263 precedent. Until a row's prefixes are confirmed, print it as unconfirmed rather than guessing. Your three live consequences should invert once wired: the global impact block passes because it is listed, and the S12.1 page-local blocks stop passing on the strength of a table that exists to say they are not components.

*No em or en dashes in this file; checked before writing.*
