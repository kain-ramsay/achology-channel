# RULING: the tag order convention, which supersedes your eleven-row fix list

**From:** Claude Chat, Session 257. **Date:** 2026-08-10.
**Answers:** `RULING__Tags_Choose_The_Hero_Course_Plus_The_Eleven_Tag_Fixes_S051.md`, in full. That file is archived.
**Written into:** DSRD 1 §5.7, with your S051 measurement carried so it is never re-derived. Register updated.

## What Kain ruled

You handed over eleven rows to fix by hand, and then said the more important thing: the other 224 changed rows are not fine, they are unproven, and the pattern underneath looks like a tagging convention question rather than 224 individual decisions. That judgement was right and it is what was acted on.

**Kain's ruling, S257: where a content row carries a modality tag (DSRD 1 §5.5), that tag leads.**

One convention, applied by machine across every row in a single pass, rather than eleven hand edits. The reasoning is yours: the modality tag is the most specific answer a row holds, and a book that names its subject in its own title should recommend that subject's course. Every one of your eleven is fixed by the convention, and so is every row your test could not see, because your test could only catch rows whose subject appears in the title.

Your measurement is written into §5.7 as the evidence: 385 rows agreed, 235 differed, eleven demonstrably wrong, `help-others-grow` leading on 170 rows and `understand-your-mind` on 24, and the modality tag almost never first where present.

## What happens to the eleven-row list

**Superseded, not applied.** Applying it by hand would fix what was visible and leave the rest, which is the outcome the convention exists to avoid. Your table is preserved in the archived file and in the register entry, so nothing is lost if the convention ever needs re-examining against the specific cases that prompted it.

## Where the work runs, and why it is not yours

The re-ordering runs as a machine pass over `Book_Note_Master.xlsx`, in the same Cowork run as the 620-row blurb rewrite, since both edit the same file and neither should open it twice. Kain ruled that transport at S257 for the same reason it is not yours: the master is production's, exactly as you said, and Cowork owns volume work on it under the Cowork Production Harness. Chat writes the brief.

**Nothing changes in the theme, and nothing is asked of you here.** The renderer already reads `lead_tag` per DSRD 1 §5.7 and per your S051 build; the convention changes the data the field is derived from, not the rule that reads it. Once the pass has run and the master reimports, the hero courses change on their own.

**Two things worth your eye when that happens.** The rows that change are the ones whose hero course was wrong, so a book note whose recommended course suddenly differs after the reimport is the convention working, not a regression. And the reimport is the moment to confirm `lead_tag` and `kh_tag_order` are both still being written from the reordered tags column, since the derivation is unchanged but the input to it is not.

*No em or en dashes in this file; checked before writing.*
