# RULING: the card review runs in the side panel, and the surface rule is corrected

**From:** Claude Chat, Session 258. **To:** Claude Code.
**Authority:** Kain, S258.
**Corrects:** `RULING__Design_Surface_Standard_And_Global_Impact_Prototype_S258.md`, section 2, which you have already read. That file's two-surface test was wrong. Everything else in it stands, including the fold-back and your Global Impact prototype export.

## What was wrong

The rule was written as a test of whether the built theme already holds the thing: if not, Chat renders in the panel; if it does, you render and Kain judges in Safari.

That test failed at its first use. The fifteen Knowledge Hub and product cards all exist in the theme, so the rule pointed a whole Chat design session at Safari, when the work is Kain and Chat designing together. Kain corrected it plainly: all design work he and Chat do happens in the side panel.

## The corrected rule

**The surface follows who is doing the work, not whether the thing exists.**

- **Design work Kain and Chat do together in Chat happens in the side panel, always**, whether or not the theme already holds the thing.
- **Safari is for judging what you have built and returned**, live, as the real thing rather than a reconstruction.

Both are ruling surfaces, and the fold-back is unchanged: whoever rendered the approved artefact writes the signed record. A panel approval means Chat saves the prototype and writes the build sheet. A Safari approval means you export the approved state as the prototype's next version, update the sheet, and file the RULING naming both, in the same session (your Rule 14, tightened at Harness v2.9).

Standing rule 16 is corrected, the `visual-design-craft` skill is corrected, and the vault principle under it is corrected. Nothing about your harness changes.

## What it means for you next session

**The S255 card-sheet commission is stood down.** You were asked to build one rendered sheet of every registered card from the live components. It is no longer the route: S259 is a Chat design session, card by card, in the panel, and each card that Kain approves gets its prototype and its build sheet written by Chat into the Card System folder, exactly as the book note card did at S257.

What you will receive instead, over the sessions that follow, is a signed prototype plus build sheet per card, arriving through this channel, to build against under Rule 4's precedence.

**Nothing is being asked of you here.** This is a correction to a rule you have already read, so that the two environments do not hold two versions of it.

*No em or en dashes in this file; checked before writing.*
