# RULING: the seventeen pieces of site-wide furniture become library components. Plus two renders needed

**DOCUMENT TYPE:** not a page spec.
**From:** Claude Chat, Session 267. **Date:** 2026-08-12.
**Authority:** Kain, in session, on your group 6. His words: "Yes to all 17 as library components, this is a long overdue decision."
**Answers:** `REPORT__The_304_Families_Grouped_For_Kains_Ruling_S055.md`, group 6 in full.

## The ruling

**All seventeen families in group 6 are library components.** Every one earns what a library component has: a prototype Kain approves by eye, a one-page build sheet carrying its values, and a row in the Component Registry. Your recommendation is taken exactly as you wrote it, and your reason is the reason it was taken: a thing on seventeen templates that nobody has ever approved by eye is the definition of the gap this census exists to close.

The seventeen, as your report lists them:

`policy-next`, `page-container`, `breadcrumb`, `icon-breadcrumb`, `policy-breadcrumb`, `help-q`, `help-q-list`, `help-popular`, `article-container`, `btn`, `btn-primary`, `policy-page`, `policy-header`, `policy-body`, `policy-lead`, `policy-title`, `policy-num`, `policy-doc`.

**Mark all of them in the registry now**, each as a library component with its artefacts printed as NOT RECORDED where they do not exist yet, which is most of them. That printed gap is the work queue and it should be visible rather than tidy.

## What this does not do

**It does not commission seventeen design sittings.** They are queued behind the Knowledge Hub page programme and they get worked in order of how many templates they touch, which puts the Where Next panel and the page width container first. Nothing here changes what you build next.

**It does not decide what any of them looks like.** The ruling settles what they ARE. Every value stays exactly as built until Kain approves a prototype, and no value is read from the code as though the code were the standard.

## The two renders, which are the real next step

Both of your questions inside this group turn on whether things actually look the same, which is Kain's eye on the built theme in Safari, not a classification either of us can reason out. The theme already holds both, so this is your surface, not the side panel.

**Render one: the two breadcrumbs.** `breadcrumb` and `policy-breadcrumb`, each on thirteen templates. Show them one above the other, on real pages at real widths, with the same trail depth where the pages allow it, so the question in front of Kain is simply whether these are two things or one thing written twice. If they are one thing, one of them dies and thirteen templates change.

**Render two: the Where Next panel.** `policy-next`, declared in five separate stylesheets and appearing on seventeen templates, which is the strongest case for duplication anywhere in the census. Show the panel as it renders on one page per stylesheet, so any drift between the five copies is visible rather than argued about. If they are identical, it collapses to one home. If they differ, Kain rules which one is right and the others follow it.

**Return both through the channel with the live links, as usual.** No values change and nothing is collapsed until he has looked and ruled.

## Still not settled, and coming to him next

Group 7's twenty-five families, and the four exceptions inside group 8 that are site-wide despite counting one template: the footer's twenty, the header's eight, the seventeen in the shared partials file, and the author card.

*No em or en dashes in this file; checked before writing.*
