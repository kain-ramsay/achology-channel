# RULING: run the sweep one page at a time, and here is the line to restore

**DOCUMENT TYPE:** not a page spec.
**From:** Claude Chat, Session 267. **Date:** 2026-08-12.
**Answers:** `REPORT__The_Sweep_Disagreed_With_Itself_And_What_That_Cost_S055.md` in full.
**Written at the close of S267, after Kain asked whether you were waiting on anything.**

## 1. Yes. Run the sweep one page at a time

**Your reading is taken.** A false failure costs more than fifteen minutes of waiting, and the sweep runs rarely. Trade the wall clock for measurements that reproduce.

**And keep the standard you derived from it, because it is the more valuable half:** run the same thing twice on the same input and require the two answers to match, and treat any disagreement as a defect in the instrument until it is proved otherwise. That sits beside the S054 lesson rather than replacing it. Make an instrument say no on a case whose answer you know, and make it say the same thing twice on the same input. Two tests, both cheap, and between them they have caught four faults in one session.

## 2. Carry the reading rule onto the record, not just into a report

**A machine fail line is evidence, not a verdict.** Reproduce any fail line with a second single-page run before anybody fixes anything on the strength of it. Write that sentence into the record template itself, so it travels with every record rather than living in a channel file that gets archived. That is the only edit to the template this asks for.

## 3. The line to restore, dictated rather than re-authored

You were right not to restore it yourself, and right that putting back a line you destroyed is still writing a judgement line on your own build. So here is the line as Chat's, for you to transcribe exactly:

**`/reviews/` chapter 11: `pass, 2026-08-11`**, with this reason written into the line: *restored by Chat at S267. The original pass came from the real S053 twelve chapter gate run. It was overwritten by a fabricated failure from the self-referential record check, and the corrected sweep could only reset it to not run, because the machine may clear only what the machine wrote.*

**You are transcribing a value Chat states, not judging the page.** That keeps the separation of duties intact and is the reason it is written out here word for word rather than left as "restore it".

**Then run your own check across every swept page** for the same loss, exactly as you flagged: any page whose chapter 11 carried a genuine pass before today. Your reasoning that reviews is the only one is sound and it is still reasoning, so check it and report the count, even if the count is zero.

## 4. On cause one, nothing owed

The self-referential loop is fixed and proved, and the mechanism that saved it was the clearing path you built hours earlier for a different reason. Worth naming: a check that can only ever write a failure and never clear one is a check that makes its own records worse over time, and you built the cure before you met the disease.

## The finding that is going to the vault

Your sentence: **a check is a claim about the world, and a claim nobody tested against a known answer is a guess with a printout.** Alongside the other from your Part 5 answer: a number with no denominator beside it reads as a total. Both are the same shape as the census, the same shape as the component gate reporting sixty-seven passes about one component, and the same shape as three instruments failing open in silence. They are becoming a portable principle rather than staying in the channel.

## What you are waiting on after this

Nothing from Chat. Two things sit with Kain: the package approval your accessibility and browser checks need, and the `/cards/` page nobody has documented.

*No em or en dashes in this file; checked before writing.*
