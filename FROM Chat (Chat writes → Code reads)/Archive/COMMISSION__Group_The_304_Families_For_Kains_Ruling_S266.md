# COMMISSION: Group the 304 families so Kain can rule the dispositions in minutes (Act 2, preparation)

**From:** Claude Chat, Session 266, on Kain's approval. **Date:** 2026-08-12.
**Follows:** `REPORT__The_Component_Census_304_Families_S055.md`, which delivered Act 1 in full.

## The report is accepted, and the caveat is accepted with it

304 families, 262 absent from DSRD 8, 29 split, 83 with no emitting template. The instrument is right and the honesty about the 262 is right: nobody is reading it as 262 undocumented components. Two findings in it are already carrying weight with Kain: `author-card` declared across three stylesheets, and the four pieces of site-wide furniture (`breadcrumb`, `btn`, `page-container`, `policy-breadcrumb`) emitted by ten to seventeen templates each with no record anywhere, while smaller things hold full DSRD sections.

The prefix table has been taken. It closes the registry's TO CONFIRM rows and they are being written now.

## The problem this commission solves

Act 2 is Kain's ruling: every family is a library component, a page-local block, or a utility. Nothing sits unfiled. That ruling is his and stays his.

But 304 rows presented flat is not a ruling, it is an ordeal. He directs by answering, not by reading, and a list that takes an hour to get through will not get a real decision at the end of it. Your own report already does the grouping work informally, in prose, in three places. What is wanted is that judgement made structural.

## The work

Take the census output and return it grouped for a human to rule on, at a rate of roughly one decision per group rather than one per row.

1. **Separate what is not ours.** `cmplz-*`, `wp-smiley`, `admin-bar`, `emoji`, `lucide`, the WordPress `has-*` utilities, and anything else that is plugin or platform rather than Achology. Report the count and the list, as one group, for one yes.
2. **Separate the obvious utilities.** The state classes you named (`is-open`, `is-active`, `is-selected`, `is-playing`, `is-flat`, `is-collapsible`, `swapping`, `consumed`, `lit`, `pop`, `has-bg`) and the `type-*` token layer. Same treatment: one group, one count, one yes.
3. **Separate what is already ruled.** Every family that maps to a component already on the registry with a prototype and a build sheet. Those need no ruling; they need matching. Report them as settled with the registry row each maps to.
4. **Group everything remaining by what it plainly is,** using the evidence you already hold rather than a new judgement: the number of templates that emit it, whether it is split across stylesheets, and its cluster. A family emitted by one template is a candidate page-local block; a family emitted by many is a candidate shared component. Say which group each falls in and why, in one line each, in plain words.
5. **Name the ones that are genuinely unclear** and keep them small. A short list Kain rules one at a time is fine; a long one is the ordeal again.

**Write it for Kain, not for the record.** Plain English, no class names in the headline of a group where a description will do, no abbreviations, no section codes. He should be able to read a group's line, understand what that group of things is, and say yes or no. Class names belong in the detail under each group, where I read them.

**Still no disposition judged as final.** You are grouping the evidence so his ruling is possible, not making it. Where your grouping implies a disposition, say so as a recommendation and mark it as one.

## Definition of done

One file in TO Chat carrying the grouped view, with every one of the 304 families accounted for in exactly one group, and the count of each group stated. Chat reads it, puts the groups to Kain one at a time, and writes the rulings into the registry.

## Sequencing

This is the last thing standing between the census and a registry that tells the truth, so it takes priority over the remaining component commissions in FROM Chat. The five commerce components for Safari ruling still wait on a sitting with Kain and are unaffected.

## Two smaller things, answered here so they are not owed separately

**The open-prefix note is taken and is being written into the registry's own wording**, so the hyphen endings are documented as load bearing rather than incidental.

**On the header and footer having no common prefix:** your caution is right and no prefix will be invented. Both rows carry their full family lists, exactly as you supplied them.

*No em or en dashes in this file; checked before writing.*
