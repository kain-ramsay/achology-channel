> **CODE DISPOSITION, S090: DONE.** One pass over the Vimeo API, fourteen videos found in folder 20119371, fourteen written, none refused. Every row read back off the API after the write and filed as `REPORT__The_Fourteen_Testimonial_Videos_Whitelisted_S090`. Nothing else on the account touched; `kaintamsay.com` deliberately left. The tool is committed as `tools/vimeo_embed_whitelist.py` rather than left in a scratchpad.

# RULING: add achologytest.com to the embed whitelist on all fourteen testimonial videos

**From:** Claude Chat, Session 311. **Date:** 26 August 2026.
**Answers:** `DELIVERY__The_Fourteen_Testimonial_Videos_S084.md`, section 3, the only decision it carried.
**Authority:** Kain, ruled in session at S311, on my recommendation.

---

## The ruling

**You make the change yourself, by the Vimeo API, in one pass, and read it back.** Not Karen by hand. Kain has authorised you to write to the Vimeo account for this one change only: adding `achologytest.com` to `privacy.embed`'s whitelist on each of the fourteen videos in folder `20119371` on user `71102328`.

Nothing else on the account is touched. Do not change `privacy.view`, do not retitle anything, do not fix the two copy faults you found in the titles: those are Kain's words to rewrite if they ever become visible copy.

## Why this was ruled rather than left

Your finding is the whole reason: the block would have been built, would have passed every gate this project owns, and would have shown fourteen dead players on the build site. Nothing in our machinery reads a Vimeo privacy setting. So the fix goes in before the block is built, not after Kain sees it broken.

## What is owed back

**A read-back, per video.** After the pass, read `privacy.embed` and the whitelist domains back off the API for all fourteen and file the printout, one row per video, so the change is proved rather than reported. If any of the fourteen refuses the write, name it and stop rather than working around it.

**`kaintamsay.com` stays untouched.** You were right to note it rather than tidy it. It is a misspelling of kainramsay.com, it is doing nothing, and it is now recorded on the testimonials card for the cutover list.

## What happens next on Chat's side, so you are not waiting on a mystery

Chat writes the fourteen-video block into the Video Testimonials page's signed spec: one question per video, the speakers unattributed unless Kain supplies who answered which, and the count corrected to 45 answers from nine members, which your reading of `page-testimonials.php` settled. You build the block to that spec, not before it.

OWED BACK: the per-video read-back printout of the embed whitelist, filed to TO Chat.

*No em or en dashes in this file; checked before writing.*
