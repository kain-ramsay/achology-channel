> **CODE DISPOSITION, S085: WAITS ON** Kain in Safari. This is the full terms of the sittings that BRIEF__The_Four_Chrome_Sittings_Are_Unparked_S302 reactivated, so it stays live beside it rather than being archived under it.

# COMMISSION: the four chrome components are Safari sittings, and they are yours to run with Kain

**DOCUMENT TYPE:** approved brief. Not a page spec. **From:** Claude Chat, Session 282. **Date:** 18 August 2026.
**Board card:** "Cards + Chrome Sweep: Review all Unreviewed Components + give each a Prototype + Data File" (Urgent and Important).
**Reads with:** `BRIEF__The_Section_Header_Component_Is_Approved_And_Filed_S282.md`, beside this file, which is the fifth component and was the only one that belonged in Chat.

---

## The decision this file carries, and why it exists

S282 opened as the chrome sweep: five components to clear Kain's eye, which between them release eleven Knowledge Hub page designs. **Only one of the five was Chat's to render. The other four are yours.**

The dividing question is standing rule 16's: does the built theme already hold the thing? Chat checked the theme rather than assuming, and it holds four of the five.

| Component | Where it already lives |
|---|---|
| Site-wide header | `header.css`, its own stylesheet |
| Site-wide footer | `footer.css`, its own stylesheet |
| Breadcrumb | `components.css`, the `.breadcrumb` family |
| Where next panel | `components.css` as `.policy-next`, promoted and locked S219, image variant locked S250, live on three pages |

**Rendering these in the Chat side panel would have handed Kain four rulings taken on reconstructions of things that already exist.** A ruling made on a reconstruction may not hold, and the drift between the reconstruction and the theme becomes a new contradiction nobody asked for. So they wait for Safari.

**This is the one piece of S282 that lives nowhere you can see it.** Everything else from that session is already filed on disk or written into FROM Chat. This decision existed only in the conversation and in Chat's own handover, which you do not read. That is why it is a file.

## What is being commissioned

**Render these four for Kain in Safari, on the live pages that already carry them, in this order.** The order is by what releases the most page work first, not by size.

1. **Site-wide header.** On every page; nothing ships without it.
2. **Breadcrumb.** Thirteen templates, and there are **two of them**, `breadcrumb` and `policy-breadcrumb`, each on thirteen templates. The component registry already names this as one of two open questions needing Kain's eye rather than a classification: **are these one thing written twice?** Put that question to him inside this sitting rather than as a separate job.
3. **Where next panel.** Seventeen templates, the largest component on the site. The registry names the second open question here: **`policy-next` is declared in five separate stylesheets**, the strongest case for duplication anywhere in the census. Same treatment: his eye, inside the sitting.
4. **Site-wide footer.**

## What each sitting has to produce, and this is the part that has been missing

**A ruling that is not filed did not happen.** Chat cannot see your window and you cannot see Chat's, so a component Kain approves in Safari is invisible to Chat until it is written down. Under standing rule 16's fold-back, the side that was in the room files it.

**For each of the four, on approval:**

- **Export the approved state into that component's design folder as its prototype.** Three of the four have no folder yet; create it, following the shape of the Section Header folder filed today.
- **Write its data file**, `COMPONENT_DATA__{component}.json`, in that folder. Use `COMPONENT_DATA__review-card.json` as the pattern, including its **gate block**: values alone cannot be compared against a built page, so every value worth checking carries its selector and property binding. The section header's data file filed today follows the same shape and includes a worked example of the gate block for a component that is not yet built.
- **Write the folder's README**, naming what is inside it.
- **File the RULING** per your harness Rule 14.
- **Tell Chat through TO Chat**, so DSRD 8 gets its decision section at Chat's next close.

**The registry's four artefacts are the standard: class prefix, prototype, build sheet or data file, and DSRD 8 section.** As of today, exactly one component in the whole project carries a complete set, and it is the section header. These four bring it to five.

## What this does NOT commission

**Do not build or change any of the four.** They are built. This is a review, and the only outcome that changes code is Kain naming something he wants different, which then travels the normal route.

**Do not design anything new.** Where a value has never been ruled, it is rendered as it is with that fact named to Kain, not filled in.

## What this releases, so the priority is visible

Eleven Knowledge Hub page cards sit at Urgent and Important. Under Kain's S281 ruling the sweep runs page by page: a page is gated on the components it actually uses. **The Article Page has five of its seven open items closed as of today and cannot be signed for one reason only: these four.** Every other Knowledge Hub page waits behind the same four.

**Five sittings were the estimate, not twenty four.** Four of them are in this file.

*No em or en dashes in this file; checked before writing.*
