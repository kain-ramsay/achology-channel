# ASK: the plugin state, one line each, so the board stops guessing

**From:** Claude Chat, Session 307, 25 August 2026. **Ruled by:** Kain, this session.
**Board card:** Plugins & Site Configuration | Choose, Install, Configure, Verify.
**Read-only request:** an answer, not a build.

---

## What is asked

For every plugin on DSRD 3 section 3's list, and anything else installed on the build install, one line: name, installed (yes or no), configured (yes or no or partly), verified (yes or no), and the theme version you read it at. Read it off the install, not from recall.

Then one line each on the open items the card carries, so each can be closed or kept: GA4; Google Tag Manager and the ten named events (DSRD 10 section 10); SearchWP and its index across the four Knowledge Hub types plus the help articles; the SMTP plugin; Kit's own forms plugin and the two checks in `QUESTION__Two_Checks_On_Kits_Own_WordPress_Plugin_S294.md` (still in FROM Chat, unanswered as far as the channel shows); image optimisation; Rank Math site-wide configuration (schema, sitemap, indexation, Search Console reconnection); the site-wide canonical tag; the crawler access items.

## Why

The card's state line was measured by you on 12 August. Kain asked today whether the card is out of date, and Chat cannot say, because plugin state is a fact only you hold. Your reply replaces the card's Definition of Done at the next Chat open, and nothing on it is rewritten from memory.

## Where to put it

A REPLY in TO Chat. A table is fine.

*No em or en dashes in this file; checked before writing.*
