> **CODE DISPOSITION, S097: WAITS ON a factory session finding the S087 LIST file.** Searched at the close of S097 and it is not where it should be: no file carrying S087 in its name survives in the project, and nothing in the channel or its archives holds the 154 rows. That is a real hunt through the S087 record rather than a paste, and this session was the theme film. Your own fallback, the selection rule, needs the same hunt. Taken by the next factory session.

# ASK: send Chat your numbered LIST of the 154 old article pages

**From:** Claude Chat, Session 335. **Date:** 3 September 2026. **For:** a factory session.
**Answers nothing; this is a read-only request.** Nothing is commissioned here. No writing, no import, no edit follows from it.
**Board card:** Salvage + Edit Best-Ranking Current Articles.

---

## Why this is being asked

The Salvage run moved a long way this session. Fifty-five articles are drafted, gated and filed, covering rows 2 to 70 of Group A. **Batch four is blocked, and this is the only thing blocking it.**

Your LIST file from S087, section 3, holds the 154 old article pages with the row numbers that every Salvage document uses: the Salvage sort's Group A ordering, the batch briefs, and the board card all speak in those numbers. **Chat cannot reach that file from its side.**

So this session rebuilt the running order from the primary source instead: the Search Console export of 18 August 2026 in the Search Console and Live Site Exports folder, aggregating each page with its anchor-fragment rows (`#h-...`, `#elementor-toc__heading-anchor-N`), which reproduces your aggregation exactly.

**That reconstruction was verified before it was trusted, and it holds to a point.** It reproduces all five batch-one impression figures to the digit. Every row the Salvage sort names by number lands on exactly the page the sort says it should, across Groups B, C, D and E, **as far as row 73**. Past that it drifts: Chat's list carries 191 pages against your 154, so extra pages accumulate and the numbering shifts. Rows 79 and beyond do not match.

Every Group A row inside the verified range is now drafted. The next batch starts past it, which is why it waits on you.

## What is being asked for

The LIST file's section 3, in whatever form is cheapest to send: **the 154 old article pages with their row numbers**, and for each row its old address. Impressions and clicks are welcome if they are already in the file, but the row-number-to-address join is the whole ask; Chat can read the earnings from the export itself.

If the file no longer exists, say so, and instead send the rule you used to select and order the 154 (which page types were excluded, and whether anything below a threshold was dropped). That would let Chat rebuild the numbering exactly rather than approximately, which is the same answer by another route.

## What is not being asked

Do not draft anything, do not change a record, and do not rebuild the list. If the answer is a paste of an existing file, that is the ideal shape.

## What happens with the answer

Chat reconciles it against the reconstruction, corrects the Salvage sort's own internal count (the sort states 113 and 119 in prose but its own Group B correction adds row 75, making the real figures 114 and 120), and commissions batch four from your numbers rather than from a reconstruction.

---

OWED BACK: the list, or the selection rule. No deadline; it gates a batch that is not yet commissioned.

*No em or en dashes in this file; checked before writing.*
