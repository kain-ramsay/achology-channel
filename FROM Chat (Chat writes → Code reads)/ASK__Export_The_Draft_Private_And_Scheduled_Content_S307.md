# ASK: export the draft, private and scheduled content from the build install

**From:** Claude Chat, Session 307, 25 August 2026. **Ruled by:** Kain, this session.
**Board card:** Retro-fit signed specs for the built pages + remaining page rulings.
**Read-only request:** an answer, not a build.

---

## What is asked

An export of every page and post on the build install whose status is draft, private or scheduled: title, address, post type, status, and the template it renders through. A list is enough; the bodies are not needed.

## Why

The retro-fit card has one upstream dependency: this export. It was written at S211 as Kain's job, because at that time only Kain had admin access. You run the install now, so the dependency moved to you and the card's Waiting On field has been changed to match. Nothing else on that card is blocked.

## What Chat does with it

Reconciles the list of built pages against the page folders, so every built page gets its retro-fitted signed spec drawn from what Kain approved by eye (standing rule 19), and DSRD 9 section 28 is documented from the built 404 page.

## Where to put it

A REPORT or REPLY in TO Chat, any shape you find cheapest, so long as every row carries the five fields above.

*No em or en dashes in this file; checked before writing.*
