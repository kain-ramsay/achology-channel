# RULING: widen H9 to see the factory folder, and count draft-only as safe

**From:** Claude Chat, Session 338. **Date:** 4 September 2026.
**Answers:** `ASK__Four_Things_Are_Blocked_And_None_Of_Them_Is_Mine_To_Decide_S099.md` item 1, and `ASK__Book_Note_Import_Is_The_Same_H9_Register_Gap_Worse_S100.md`.
**Board card:** the harness card.

---

## Kain's ruling, in full: yes to both halves

**One. `reviewed_scripts()`'s folder lookup widens.** It checks the theme folder and `theme/tools` today. It now also checks the Content Production Factory folder, where `import_instructor_articles.py` actually lives.

**Two. The register's own qualifying test widens.** `h9_reviewed_scripts.json`'s `_how_to_add_one` said a script must "cannot create or publish a post." That sentence is corrected to: a script qualifies if it **cannot publish a post itself**, whether or not it creates one as an unpublished draft. A draft still needs Kain's own click before anything is live, so a draft-only script carries none of the risk H9 exists to stop.

## What this clears right now

**Register both scripts already read and ready:**
- `import_instructor_articles.py`, read and hashed at Code's S098.
- `tools/book_note_import.py`, hardened to draft-only this session, commit `9cfc40c`, sha256 `f1742f3074ce977033bcecdf106c828fb2b4f144fea34106b1c5d03e54ef9a8c`.

Once registered, the three held instructor articles and the 75 not-live book notes are clear to import as drafts.

## One thing Chat believes but has not confirmed

The 55 rescued Salvage articles are drafted and gate-clean, and Chat believes this same ruling frees their import too, if their own import script sits in the same Content Production Factory folder. **Confirm that against the actual script rather than assuming it.** If a different script, or a different fault, is what is actually holding those back, name it back rather than folding it into this ruling.

## What does not change

Nothing publishes on its own. This ruling only lets the wall see and trust scripts it was blind to; a human click is still what makes anything live.

---

OWED BACK: both entries registered, the three instructor articles and the book notes moving as drafts, and a plain yes or no on whether the Salvage import is covered by this or needs its own answer.

*No em or en dashes in this file; checked before writing.*
