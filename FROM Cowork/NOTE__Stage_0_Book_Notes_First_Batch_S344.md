**From: Claude Cowork**
**Date: 5 September 2026 (S344)**
**Re: Stage 0 demand check on the 67 published book notes -- first batch of 8, in progress, informational**

Working `BRIEF__Run_The_Stage_0_Demand_Check_On_All_65_Published_Book_Notes_S341.md` and its two addenda (S342, S343). First batch of 8 rows done, live search per row, records edited, content_gate.py re-run against each and confirmed clean. `CORRECTION__65_Book_Notes_Stage_0_Results_S341.csv` started in `Content Records/book-note/` (currently 8 of 67 rows; file's own header comment says so, and will keep saying so honestly until the run is complete -- do not read the partial file as the finished deliverable). Continuing through the remaining 59 in further batches; the DONE file and final count only go to FROM Cowork once all 67 are in the table, per the brief.

**Six rows stood unchanged, evidenced:** a-guide-to-rational-living, why-zebras-dont-get-ulcers, journey-to-the-heart, homage-to-catalonia, the-bridge-across-forever, mans-search-for-meaning. Each now carries a real `demand_evidence` entry (was the pre-S329 placeholder on five of the six; mans-search-for-meaning already had a proper one from S338, untouched).

**the-bridge-across-forever** also got the missing internal link named in the S343 addendum's job 5 (Code's Rank Math read on the live page flagged it short one): added one link to `the-relationship-cure` (live book note, matching theme) inside the "What Could this Mean for Society?" paragraph, within the paragraph's existing sentence budget. Nothing else in the body touched.

**Two rows carried out the S342-ruled "by" fix that hadn't reached the record yet:** free-will-sam-harris (`free will sam harris` -> `free will by sam harris`) and nature-emerson (`nature emerson` -> `nature by emerson`). For both: `prod_rm_focus_keyword`, `prod_rm_seo_title` (matching the addendum's own stated titles and character counts exactly, 57 and 51), `prod_rm_seo_description`, `prod_cover_image_alt`, and the opening line's phrasing (a one-clause fix inside the existing first sentence, not an added sentence, so the exact phrase now lands at first use) were all updated. `KEYWORD_REGISTER.csv` updated to match on both rows, with a note on what changed and why.

Re-gating both after the fix surfaces exactly the outcome the S341 brief itself anticipated: **`keyword in address slug` now FAILs on both** (`free-will-sam-harris` and `nature-emerson` cannot carry "by" -- the slugs are fixed, per the brief, and this run does not rename them). Recorded in the table as `slug_state: does not carry keyword`, per the brief's own instruction that this is Chat's call, not Cowork's. Everything else on both records gates PASS. Keyword density on both stays below band (0.44% and 0.23%): the addendum authorised only the one opening-line fix as this run's body edit, not a fuller lacing pass, so both are marked `keyword_state: changed, body pass owed` in the table, honestly reflecting that the fuller pass is a separate, smaller job still to come, per the brief's own framing.

**One older loose end surfaced, not this run's to fix:** mans-search-for-meaning's own `demand_evidence` field (written S338) already documents that its natural keyword, "man's search for meaning", is claimed in the register by a different page, `finding-lifes-purpose-with-viktor-frankls-mans-search-for-meaning` (field-authority-article), and flags that Kain should rule whether that article keeps the fuller phrase or one of the two pages moves to a longer-tail variant. No sign this was ever ruled on. Left exactly as found; the register is Chat's to change, and the ruling itself is named to Kain separately today.

Nothing else touched. No post_title, no SEO title pattern beyond the two named exceptions above, no other record's body.
