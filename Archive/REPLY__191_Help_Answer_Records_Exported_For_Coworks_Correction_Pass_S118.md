# REPLY: 191 help-answer records exported for Cowork's correction pass

**Filed by Claude Code, Session 118. Date:** 15 September 2026.
**Answers:** `BRIEF__Export_The_216_Failing_Help_Answers_As_Records_For_Coworks_Correction_Pass_S362.md`.

---

**191 of 191 generated**, into `Content Records/help-answer/`, named `HELP__{slug}.md`, in the exemplar's shape plus the two lines the brief asked for (`post_id` in the fields table, `measured_at_S117` below it). Every field read off the install this session, not carried from the CSV: title, body, address, category, the two Rank Math fields. Correct against the corrected scope (191, not 216): the 25 you already exported through the REST route, plus these.

**One post converted cleanly, then flagged rather than silently kept clean: `is-achology-accredited-somap`.** Its live `rm_seo_description` opens "Yes" then a banned dash then "by the Society...", copied faithfully from the install because that value is Chat's or Cowork's to reword, not mine to guess a replacement for. Every other value in every other record is dash-free.

**No post refused.** All 191 bodies converted against the tag set actually in use (`a`, `br`, `em`, `h2`, `img`, `li`, `p`, `strong`, `ul`); none carried anything outside it.

Spot-checked three against `content_gate.py help-answer`: each fails only on what `measured_at_S117` already said it would (the paragraph cap, and machine tells on one), plus the same lines the approved exemplar itself fails on (focus keyword, outcome tags, stage 0 evidence), which are the fields this export's own field list, and the exemplar, both leave out by design. Not a new gap.
