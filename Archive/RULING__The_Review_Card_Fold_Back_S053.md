# RULING: the review card is approved, and its fold-back is done

**From:** Claude Code, Session 053. **Date:** 2026-08-10.
**Authority:** Kain, on the live page at the close of the session: "I'm totally happy with what you have produced up until now."
**Reads with:** `RULING__The_Reviews_Archive_S053.md`, which you have already archived. Every value Kain ruled today is in it; this file records only the fold-back it left owed.
**Live:** theme v0.49.0, https://achologytest.com/reviews/

## 1. Why Code wrote this record rather than Chat

A Safari approval, so the signed record is Code's to write, per Harness v2.9 Rule 14 and your `CORRECTION__The_Surface_Rule_And_The_Card_Review_S258.md`: the surface follows who did the work. Both writes, named as the rule requires.

## 2. The two writes

**1. `achology-review-card-proof-v1.html`**, in the Card System folder inside Component Design Prototypes.

**Exported, not rebuilt**, and that distinction is the point. The markup is the live REST endpoint's own output for eight real reviews, and the CSS is sliced out of base.css and reviews.css by their section markers, so the file cannot drift from the theme by having been retyped from it. It carries the live site's fonts and the masonry script, and renders at the 880px reading column, two across, which is the arrangement Kain locked.

Opened and checked standalone before filing rather than assumed: 8 cards, 428px wide, real Como and Source Sans 3, the link at #B8460F with no underline, the 18px arrow, all 8 course images loaded, 8 star rows.

**2. `BUILD_SHEET__review-card.md`**, beside it, to the book note sheet's shape.

## 3. One finding worth carrying into the S259 card sessions

**The sheet has no UNKNOWN rows,** and that is the only structural difference between it and the book note sheet.

Because this prototype is the live page's markup exported unchanged, its class names ARE the theme's, so section 2 could be measured this session rather than reconstructed later. The book note sheet's section 2 is still carrying UNKNOWNs precisely because its prototype was drawn before the build.

The general shape of it: **a prototype exported from a build carries its own section 2 with it; a prototype drawn before the build owes one.** Not an argument for changing how S259 runs, since design has to happen before code exists. It does mean that when Chat's panel-approved prototypes arrive here, filling their section 2 is a real act Code owes on each one, not a formality.

## 4. The folder's README is updated, and one thing there is owed

Updated per the README's own rule 3: both filenames, the corrected surface rule, and the note that the folder now holds a card that is not a Knowledge Hub card.

**The folder is not under git.** Act 1 of `COMMISSION__Build_Vs_Sheet_Gate_S257.md` commissions that so superseded prototype versions live in history rather than as parallel files, and it has not been done. Until it is, the README's rule 1 is a convention rather than a mechanism, and a v2 of anything in there would have to overwrite a v1 with nothing holding the old one. I have written that into the README rather than leaving it as a silent gap, and it is the first thing I would do when that commission comes round.

## 5. What is signed, and what is not

**Signed:** the review card, as it stands at v0.49.0.

**Not signed, and still open on the block around it:** the standouts above it are unbuilt because the Featured flag is empty on every export row; `review_title` and `review_theme` do not exist yet and are waiting on the tagging pass; the three archive copy lines are draft; the search and filter bar is next, under your `BRIEF__Reviews_Control_Bar_Search_And_Dropdown_Design_S258.md`; and the capitals question is untouched, so names stay exactly as the students typed them.

*No em or en dashes in this file; checked before writing.*
