# SHIP: v0.39.1 and v0.39.2

**From:** Claude Code, S050. **Date:** 2026-08-07. Both deployed over SSH, cache purged, both verified serving 200.

## v0.39.1: the Book Note page template

The full record is `RECORD__Book_Note_Page_S050.md` beside this file, including the DSRD 6 chapter-by-chapter table, the page gate printout, four recorded exceptions, four items waiting on your ruling, and six places the documents and the approved page part company. This is the one-screen version.

**What changed.** New `single-book_note.php`, `book-note.css`, `book-note.js`. Changed `base.css`, `components.css`, `shared-parts.php`, `knowledge-hub-setup.php`, `learn-listing.php`, `group_book_note_fields.json`, `people-setup.php`, `functions.php`.

**Live:** https://achologytest.com/learn/psychology/book-notes/mans-search-for-meaning/

**Gates:** `css_gate` PASS on all four stylesheets touched. `page_gate` 13 passed, 7 failed, of which 4 are recorded exceptions with quoted authority and 3 are the open questions. Zero em dashes and zero en dashes on the rendered page.

**Four defects were found on the rendered page rather than in review**, which is the argument for rendering before recording:

1. All three closing-panel rows opened in a new tab. `achology_routes_grid()` decides externality from whether the URL carries a scheme, and I passed `home_url()`, which does. Every other caller passes root-relative paths; now so does this one.
2. An unknown book slug answered 200 and rendered somebody else's category listing under the address. Now a real 404.
3. The note body rendered at 16px against DSRD 9 §32.4's 17px, because a site-wide paragraph rule outranks a font-size set on an ancestor.
4. The separator was an `<hr>`, which puts a third element at the boundary so the page reads as two boundaries. Rebuilt as a `border-top` carrier, which is the mechanism DSRD 7 §4.3 actually describes.

**What I need from you**, in priority order: a home for `.bn-sep` in DSRD 8 or an exception for page-level separators at the gate's check 4; a ruling on the related-courses block, which cannot be built because no course pages and no DSRD 5 read exist; the rating-tick scale; and `/learn/authors/{slug}/` in DSRD 1's planned-URL table.

**One thing you should see even though it is not my page.** `single-article.php` ships two hardcoded course cards with invented prices and student numbers, under a comment saying they "render until course pages exist to query". That is live on the article page. I did not touch it, because it is another page and Rule 3 says so, but it needs its own pass.

## v0.39.2: the three people.css 640px annotations

Executes `RULING__People_CSS_640px_Stack_Points_Approved_S252.md`, which arrived mid-session and which hook H6 made me read before it would let the next edit through.

`css_gate people.css`: **PASS**. That was the last gate failure standing between the Our People record and the ten profile records behind it, so those are now unblocked.

Two stylesheets still fail `css_gate` and neither was touched here: `cards.css` (7 issues) and `testimonials.css` (24). Both pre-existing, both reported rather than swept.

## v0.39.3: two spacing defects Kain caught by eye

Recorded in full at §10 of the record. In short: the gap under the breadcrumb measured 96px because the shared breadcrumb and the hero grid were both supplying it, and the closing panel sat 0px above the footer where DSRD 7 §4.3 ruling 5 wants 48. Both corrected, both re-measured at 1440 and 375.

**Worth your attention as a gate question.** `page_gate` passed hairline-spacing at all three widths before and after, because both defects sit at boundaries where no hairline is drawn. It cannot currently see a page whose last block touches the footer, nor a boundary where two elements each contribute space, which §4.3's own first sentence makes a defect however the total measures. Kain found both by looking. Whether the gate should learn them is yours to weigh.

## Also filed this session

- `MISSES__ISBN_And_Cover_Run_S049.md` and the closing section of `RECORD__ISBN_And_Cover_Run_S049.md`: the S250 commission finished and verified against the image folder rather than against its own record.
- `ANSWER__Asset_Folder_Paths_S050.md`: two references to the dissolving folders, both comments, both mine, both listed with their exact strings. Nothing executable breaks.
- `RULING__Book_Note_Brief_Lives_In_Its_Page_Folder_S050.md`: filed under Harness Rule 14.

*No em or en dashes in this file; checked before writing.*
