# DONE: all 24 DSM articles are live, and all 24 passed the check on their real addresses

> **CHAT DISPOSITION, S365: READ AND CLOSED.** Record only; the 24 DSM articles are live. Kain's 86/88 clearance is written into DSRD 6 section 5 item 11. Board: 24 DSM articles card, not re-read this session (named in the S365 handover).

**Filed by Claude Code, Session 119. Date:** 15 September 2026.
**Answers:** `NOTE__The_24_DSM_Hero_Images_Have_Landed_As_One_Zip_Unzip_Map_Attach_S362.md` and `BRIEF__Import_And_Score_All_24_DSM_Series_Records_As_Drafts_Publish_Nothing_S357.md`, whose "publish nothing" is lifted by Kain's ruling this sitting, filed as `RULING__Kain_Clears_The_24_DSM_Articles_At_86_And_88_S119.md`.

---

## What is live

All 24 sit under `/learn/psychology/articles/`. Read back off the install: 24 of 24 `publish`.

## The live check, run at once, per the S362 first-publish route

Every one of the 24 was fetched on its own real address, as a browser, and read back. **24 of 24 answer 200. Every one carries its own hero image and its own page title. Bodies run 2,963 to 3,521 words as rendered.** Nothing failed, so nothing was taken down.

One note on the fetch, recorded because it will bite the next person: an anonymous read with a plain library user agent is answered 403 by the host on every address. A browser user agent is answered 200. That is the host's bot protection, not the pages, and it is the same shape of obstacle `score_run.py` records having cleared at S085.

## Why Kain published them rather than Code

Code could not, and did not pretend otherwise. `publish_gate.py` is the only publishing route Code has, and its volume route (DSRD 6 §0) refuses unless the page type's signed exemplar carries no failing line. The article exemplar, I10 `why-giving-advice-does-not-work`, carries one: chapter 9, Speed, `fail, 2026-09-15`, FCP 3.0s against a 1.5s target and LCP 3.5s against 2.0s, measured by Code at S117 and routed by Chat to the launch performance card as a site-wide question.

That refusal returns before `--override` is read, so Kain's ruling could not be carried through the flag the gate provides for exactly that purpose. **Code did not convert the speed line into a recorded exception to get past it.** DSRD 6 §0's definition of an exception is "Genuinely does not apply", and a real site-wide page-speed fault does apply, so writing one would have been a false readiness record of the precise kind this project's harness exists to prevent. Kain was told that plainly and published the 24 himself in the WordPress admin in four steps, which no gate here touches.

**Two things Chat should see in that.** First, DSRD 3 §4.4 says of speed: "Run PageSpeed Insights on a representative page of each type as the new build comes together, and again across the site before cutover. If any metric is outside target, investigate before going live", with the SiteGround Dynamic Cache named as a dependency, "Pooka & Co task, before cutover". So the standard times the speed investigation at cutover, while the publish gate treats the same line as a blocker on publishing to a build ground that is hidden from search. Those two readings disagree, and the disagreement is what put Kain's hands on the keyboard today. It is a question for Chat, not a judgement Code will take.

Second, and unchanged from this session's earlier file: **no book note page has a DSRD 6 record at all**, so the same wall stands in front of every book note, with nothing even to point `--exemplar` at.

## One fault found on the live pages, pre-existing, not caused by this publish

Every article page carries an empty `<h1></h1>` element. Checked against `persuade-someone-who-disagrees`, live since well before today: it carries the same empty heading, so this is a theme fault of long standing and not something today's import introduced. It is a theme edit, so a factory session may not make it: queued as its own line in `000__THE_THEME_QUEUE.md` this session.

OWED BACK: Chat's reading of the speed disagreement above, and the book note exemplar record.

No em or en dashes in this file; checked before writing.
