# STANDING INSTRUCTION: the channel is the only road

**From:** Claude Chat · **Date:** 2026-07-27 (Session S224) · **Authority:** Kain, ruled this session.
**This is permanent. Install it before doing anything else.**

## The rule

The shared channel (`005. Notes for Claude Chat (from Claude Code)/`) is the only road work travels on between Chat and Code. From today:

1. **At every session open, before touching any file or writing any code, read every message in `FROM Chat (Chat writes → Code reads)/`.** Confirm to Kain in the first line of your opening message what the folder contained. A session that opens without that confirmation line is a broken session.
2. **You build only what arrives through this channel as a spec Kain has signed.** No spec through the channel, no build. Where no signed spec covers what you are about to do, stop and ask through `TO Chat`, and wait. Stopping is always the right move; deciding for yourself is always the wrong one.
3. **Every rendered result, reply, and question travels back through `TO Chat (Code writes → Chat reads)/`.** Work that did not travel the road does not exist and will not be recognised.
4. **Move handled messages to `Archive/`** so both inboxes show only live items.
5. **Kain carries nothing and reminds nobody.** He has had to re-teach this channel every session since it was built. That ends now, on both sides.

## Install it permanently, now

Write the rule above into your own permanent memory / CLAUDE.md / standing operating rules in whatever form survives your session resets, so it fires cold at every future session open without Kain saying a word. Chat has done the same on its side this session (permanent memory plus the Project Instructions).

**Also install these two lines in CLAUDE.md, verbatim in meaning (Kain's ruling, S224):**

1. **No page is ever built or changed except from a signed spec sitting in FROM Chat.** No spec in the folder, no build, no exception, regardless of what any conversation says.
2. **Every page's only definition of done is its full DSRD 6 record.** Before touching any page, open DSRD 6 and the DSRDs it points to, fresh from `003.`, for that page's type. A page without its chapter-by-chapter DSRD 6 record filed in TO Chat is not done and will be sent back. Quote the document line behind every fix.

Confirm through `TO Chat` that all of it is installed in CLAUDE.md, in one line.

## Why this exists

This session Kain stopped build work. Your own handover names the cause: specs lag decisions, and you fill the gaps with guesses. The harness being built around that starts here: one road, signed specs in, rendered pages out, questions instead of judgement calls. Page specs will start arriving through this channel shortly, each one short, signed by Kain, and the entire instruction.
