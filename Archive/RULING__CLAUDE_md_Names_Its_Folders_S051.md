# RULING: CLAUDE.md's four stale folder numbers are corrected, by naming

**From:** Claude Code, S051. **Date:** 2026-08-08.
**Filed under Harness Rule 14.**
**Companion to:** `REPORT__Every_Old_Folder_Number_Found_S051.md` section 3.

## What was put to him

Reported from the path search: four of the old folder numbers were in his own
standing instructions file, the one that loads at the start of every session.
Asked, in one question:

> "Shall I correct those four?"

## Kain's ruling, his word in full

> "yes"

## What was done

Corrected by **naming** the folders rather than renumbering them, because that
is what the file's own standing rule 24 requires, and renumbering would only
have set the next rename up to break them again:

| Line | Was | Now |
|---|---|---|
| 8 | root of `005. Notes for Claude Chat (from Claude Code)/` | root of the Notes for Claude Chat folder |
| 42 | `005. Notes for Claude Chat (from Claude Code)/FROM Chat (Chat writes → Code reads)/` | the `FROM Chat (Chat writes → Code reads)/` folder inside the Notes for Claude Chat folder |
| 154 | the `003. DSRD's \| Achology Specification Documents` folder | the `DSRD's \| Achology Specification Documents` folder |
| 155 | inside `001. Project Delivery System` | inside the Project Delivery System folder |

Re-searched afterwards for all thirteen strings: **CLAUDE.md is clean.**

The inner folder names are kept as written, since those did not change and are
what someone reads to find the place. Only the numbers are gone.

## A second ruling in the same exchange: the rule count

Found in the same paragraph and raised separately rather than folded in:
**line 10 said "Its ten rules". The Harness is at fourteen.**

Put to him as its own question, "Want me to fix that too?". His word in full:

> "yes"

**Corrected by removing the count, not by updating it.** Line 10 now reads
"Its rules, and the live contents of both channel folders, are printed into
context automatically by hook H1".

The reasoning, which is the same one that took the folder numbers out of the
harness scripts an hour earlier: a number written into prose goes stale the
moment the thing it counts moves, and this one had been wrong through four
versions of the Harness without anyone noticing. The document is the count.
Nothing now has to be remembered when Rule 15 arrives.

That is the third stale written number found in this session, after the two in
the scripts. Worth a look at wherever else a count is written into prose.

*No em or en dashes in this file; checked before writing.*
