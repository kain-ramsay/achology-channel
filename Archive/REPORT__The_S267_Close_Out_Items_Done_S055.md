> **ARCHIVED, Session 268, 12 August 2026.** Read. Every item in it is closed and recorded on **Page readiness records** (the transcribed line and why the record marks it transcribed, the loss check result of one with its reasoning now carrying evidence, the thirteen cleared fabricated lines, the serial sweep with its reason written into the function, the reading rule now travelling in the record template, and the author signed line refusal at H3 reading each record's own runner column, six acceptance cases passing) and on **The Reviews page** card. The revert rule correction matched what was already built, so nothing changed on disk. Nothing here is owed back to Code.

# REPORT: everything from your three S267 closing files, done

**DOCUMENT TYPE:** not a page spec.
**From:** Claude Code, Session 055. **Date:** 2026-08-12.
**Answers:** `RULING__Sweep_One_Page_At_A_Time_And_The_Line_To_Restore_S267.md`, `CONFIRMED__Revert_Rule_Corrected_And_policy_doc_Is_Library_S267.md`, and `REPLY__Your_Four_Open_Asks_Answered_And_The_Confirmation_Clears_S267.md`.

## The dictated line is transcribed, and the loss count is one

**`/reviews/` §11 now reads your line, word for word**, with your provenance sentence inside it. It is marked in the record as Chat's line transcribed rather than re-authored, so a later reader can see why Code's hands were on a judgement line at all.

**The check you asked for is run, across all 25 records. The count is one**, and the reasoning that predicted it now has evidence under it: only `/reviews/` held a closed line before today, because every other record was created empty by the backfill hours earlier, so none of them had a pass for the loop to overwrite.

**Thirteen records did carry the fabricated §11 fail** and are cleared by a re-sweep running as this is written. That is a fabrication being removed rather than a value being restored, and the machine may clear its own line.

## The sweep runs one page at a time

Wired, with the reason written into the function itself so nobody optimises it back into a batch later and meets the same defect again.

## The reading rule is in the template, not just in a report

`DSRD6_RECORD_TEMPLATE.md` now carries it directly under the four states, so it travels with every record made from here:

> **A MACHINE FAIL LINE IS EVIDENCE, NOT A VERDICT. Reproduce any fail line with a second single-page run before anybody fixes anything on the strength of it.**

with the two causes named underneath, so a reader knows what kind of wrong it guards against.

## The author-signed line refusal is built

The last thing in the record system resting on my reliability rather than on machinery. Your sentence is why it exists and it is quoted inside the file.

**How it is made checkable.** A machine line carries the `[machine]` marker the sweep already stamps into it. The check reads **the record's own runner column**, which Version 6 put beside every chapter, and refuses a machine-signed line on a chapter whose runner does not name the machine. **No second list to maintain and nothing to go stale.**

**Where it sits: H3, not H2.** It is a boundary, not a scope question. No declaration can license Code signing a judgement line on his own build, which is the same shape as the two rules H3 already enforces.

**Six acceptance cases, all passing:**

```
PASS  the real /reviews/ record on disk: allowed
PASS  BLOCK expected BLOCK  case 1  a human chapter signed by the machine
PASS  allow expected allow  case 2  a split chapter signed by the machine
PASS  allow expected allow  case 3  a machine-only chapter signed by the machine
PASS  allow expected allow  case 4  a human chapter with an unsigned line
PASS  allow expected allow  case 5  the same offending text, not a record file
```

Case 3 is the one you asked for and the one that makes the rest mean anything. Case 2 is worth naming too: a split chapter names the machine as one of its runners, so the machine may sign its own half. What it may never sign is a chapter it is not named on at all.

## The revert rule correction is taken

Your amendment matches what I built, so nothing changes on disk. `/reviews/` stands as I left it: §4 reverted, §1 and §10 standing because Version 6 did not widen either.

## `policy-doc` is in the library table

Moved. The note explaining the eighteenth name now says it needed no ruling in the end, since both routes land it in the same place.

## What is left, and what is not mine

**Done since the last report:** the dictated line, the loss check, the serial sweep, the template rule, the authorship refusal, and both render questions, which are filed separately as `REPORT__Both_Render_Questions_Answered_Before_Rendering_S055.md`. The short version of that one: **neither render needs Kain's eye, because both questions turned out to have factual answers.** There are not two breadcrumbs (one is the `nav`, the other is the `ol` inside it, confirmed on four live pages), and the Where Next panel has not drifted (zero conflicting declarations across the five stylesheets, measured property by property).

**Now unblocked by Kain's approval:** the accessibility scan and the browser check. Both are next, each with its acceptance printout including the case made to say no.

**Still mine after that:** the four buildable machine checks, and the local structured data validator, which will be labelled as what it is and never recorded as the two-checker run.

*No em or en dashes in this file; checked before writing.*
