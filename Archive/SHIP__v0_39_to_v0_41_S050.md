# SHIP: v0.39.0 through v0.41.0, and the S050 close

**From:** Claude Code, S050. **Date:** 2026-08-07. All deployed over SSH, cache purged, verified serving.

## What shipped

| Version | What |
|---|---|
| v0.39.1 | The Book Note page template, all 598 notes: hero, travelling shelf, both per-book listing routes, Book schema, six new ACF fields |
| v0.39.2 | The three `people.css` 640px stack points annotated, per Kain's S252 ruling. That cleared the block on the Our People record and the ten profile records |
| v0.39.3 | Two spacing defects Kain caught by eye: 96px under the breadcrumb where two blocks both paid for one gap, and a closing panel sitting flush against the footer |
| v0.40.0 | The course block and the shelf's second CTA, which the first build shipped without. `courses-setup.php` created as the courses' one home |
| v0.40.1 | The course row moved onto DSRD 8 §7.0's own grid, three cards, real DSRD 4 checkout URLs |
| v0.40.2 | The three responsive tiers corrected to Kain's clarification: three cards, two, two stacked |
| v0.40.3 | The course card's auto margin moved to the CTAs per DSRD 8 §7.2 |
| v0.40.4 | `cards.css` passes its gate; three one-offs approved by Kain |
| v0.41.0 | The article page stops inventing course data. Both pages now use the DSRD 1 §5 tag mechanism |

Plus, outside the theme: the harness library made anchor-finding, the hook wiring made rename-proof through a single launcher, and the acceptance test that proves hooks fire through their real invocation path.

**Gates:** `css_gate` 13 of 14 PASS. `testimonials.css` still fails on 24 pre-existing issues, untouched and awaiting Kain's approval, not my annotation. Hook suites: 7 of 7 direct, 8 of 8 through the real wiring.

## The two decisions S051 opens on, both yours to put to Kain

**1. The course tiebreak.** The rank rule resolves cleanly on 356 of 620 book notes and ties on 264, up to thirteen ways. Numbers are in `REPORT__Course_Tagging_Explained_Back_And_Measured_S050.md`. **Until it is ruled, the course block renders nothing on any tied page**, which today includes the Frankl book note and any article with tags that do not overlap.

**2. The tag slug conflict.** DSRD 1 §5.6 versus the 36 live terms and 620 tagged rows. Three tags, 243 rows.

## Still owed by me, carried into S051

The keyphrase 80-point report before any slugs are set; the Reviews page schema decision for DSRD 10; three chrome re-verdicts; the remaining collapse passes; the Testimonials DSRD 6 record.

## The three days' worth of self-inflicted work in this session, named once

The article page invented prices because nobody opened DSRD 5. My course companion picked by school because nobody opened DSRD 1 §5, and your own ruling made the same error on the same question an hour later. The rename took the enforcement layer down because my sweep covered the whole project and missed the one file outside it that launches the hooks.

Three instances of the same thing: **an answer the system had already given, re-derived instead of retrieved.** You have taken it to the vault and it belongs there.

The fourth instance is the one I would keep above the others, because it is the one that fooled us both: seven acceptance printouts, all honest, all green, none of which could have gone red for the failure that then happened.

## Two test posts to delete at import

`book_note` 10901 `mans-search-for-meaning`, and `article` 439, whose tags were changed during verification and no longer describe it.

*No em or en dashes in this file; checked before writing.*
