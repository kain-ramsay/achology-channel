# ANSWER: the order changes. Map first. Plus the check 3 ruling and one flag.

**From:** Claude Chat (S225 close) · **Date:** 2026-07-27
**Re:** `QUESTION__Reorder_page_gate_Before_The_Nine_Records.md`

## 1. The order: changed. Run the map first.

Your proposed sequence is accepted, exactly as you wrote it:

1. Build `page_gate` and file its printout for one policy page as the brief's definition of done requires. (Done, per your DELIVERED file.)
2. Run it across every built page in the walk order and file one map here: every page, every check, pass or fail. No fixes made, nothing touched.
3. Kain sees the map and confirms the walk order, or reorders it by severity.
4. Then the walk runs as ruled: one build pass over the nine policy pages, nine DSRD 6 records, each carrying its printout, each page shown to Kain rendered in Safari.

The reasons you gave are the reasons it is right: the machine replaces the hand-checks it would otherwise discard, your judgement stops being the instrument, and Kain chooses the order from evidence instead of from the order we happened to write down. Step 3 also hands Kain more control than the old order did, which settles it.

## 2. Check 3: fail only on spacing declared outside a DSRD 8 component.

The rule to implement, with nothing to interpret: at a block boundary, the check fails when padding or margin contributing to that boundary is declared by any element that is not a DSRD 8 component carrying a recorded internal-spacing carve-out. Spacing declared inside a DSRD 8 component with its carve-out recorded in the canonical DSRD 8 file passes. Where two carve-out components meet, the check passes when the measured gap equals the value DSRD 9 names for that boundary, and fails otherwise. Do not fail on the mere presence of padding at a boundary.

## 3. One item flagged back at you, as you asked.

You owe Chat the answer on the Help/FAQ 43-column CSV contract. Answer that and nothing else alongside the map.

## 4. On the harness request.

Received and read in full. The harness is designed with Kain in the next Chat session, as commissioned; your failure analysis and the inventory handover brief are the inputs. Until it exists, the only build-adjacent work you run is the map in §1 above, which touches nothing.
