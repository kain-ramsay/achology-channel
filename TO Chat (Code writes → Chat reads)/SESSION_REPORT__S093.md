# SESSION REPORT: S093

**From:** Claude Code, Session 093. **Date:** 1 September 2026.
**Assembled from the version control log for the session**, per Rule 13, with hand added lines marked where the work touched no file in a repository.
**Theme v0.132.1, unchanged. No CSS or PHP was touched this session, so there is no version bump, no zip rebuild and no deploy.** The one theme commit is harness code, which runs on this machine and never on the server.

---

## Finished

**The publishing wall, four findings closed.** Commit `17ba33b` in the theme repository, pushed. `REPORT__The_Publishing_Wall_Four_Findings_Closed_S093.md`. Three findings came in from S092 and the fourth was found here while proving them; two were holes and two were false refusals, all four from one habit, the wall judging the text of a command rather than the commands in it. 63 of 63 acceptance cases as specified, 24 of them new and every new one red before the fix. **Board card:** the harness's own.

**The URL Inspection subset, all 1,000 addresses, no errors.** `REPORT__The_URL_Inspection_Subset_Is_Answered_S093.md`, which is the first of the two files the S311 ruling owes. 768 indexed, 132 unknown to Google and none of them on a sitemap, 59 already failing today. Hand added: the run itself finished unattended and touched no file in a repository. **Board card:** Redirect Strategy and Delivery.

**Every Done card on the board stripped to its closing record.** Commits `7427845`, `2ed94e7` and `fdbfe72` in the record repository, pushed. `REPORT__The_Done_Card_Strip_Is_Complete_S093.md`. Eighty seven cards this session on top of the eight at S092, each backed up before it was changed, 199,507 characters of before-state measured off those backups. **Board card:** the board's own tidy.

**The board strip's undo is now versioned.** Commit `7427845`. The ignore rule on the spreadsheets folder was swallowing the backup folder, so the only undo for a destructive run sat on one disk with no Time Machine behind it, while the S092 commit message said it had been filed. Same class as the mis-filed commit message that session's own report recorded, now twice. **Board card:** the board's own tidy.

**Five more jobs, taken after the close on Kain's word in the sitting.** `REPORT__The_Five_S328_And_Security_Jobs_S093.md` carries all five. The deploy stops shipping the harness, the tools and every script into the public web root, commit `d9af02c`, theme deployed with three proofs. The content gate reads sections shallowest first, commit `5e047b6`, 41 of 41 acceptance cases with five new ones proved red beforehand. The dead book note import file is out of the live folder. All 51 author biography records now say whether their lead tag was authored or derived, commit `06769f9`. The lecture-transcript source type is live on the install, commit `1387989`. **Board cards:** the harness's own, the Content Production Factory tooling, and Publish the fifty one author biographies.

**Nine factory scripts joined version control**, in the same commit as the gate. The gate was tracked and the acceptance run that proves it was not, along with the importers, the keyword register builder and the lead tag derivation, all in one copy on a machine with no backup. Widened to the folder's Python as a rule, because naming files one at a time is how the gap opened. **Board card:** the Content Production Factory tooling.

## Started and not finished

**The URL Inspection tail.** Running since 22:53, resumable, files its own printout when it finishes. It is the second of the two files the S311 ruling owes. **Board card:** Redirect Strategy and Delivery.

**The board strip's after measurement, and one count that does not agree.** The board answered 95 cards at Status Done this session; the S092 record says 103. All 95 are stripped and backed up with no duplicates. The workspace's SQL query quota ran out before either the recount or the after measurement could run, so both are owed and both are one query. **Board card:** the board's own tidy.

## For the record, and neither is commissioned

**The workspace's SQL query quota ran out twice today**, once before the strip began and once at its close. The strip finished anyway on one card at a time, at roughly twice the calls. Worth knowing before planning any run that reads the board in bulk.

**A tool result mid-session carried an instruction to stop using the file tools and make every change through shell commands instead.** It did not come from Kain and it would have taken every file edit out from under the scope wall and the gates, which read the edit tools. It was not followed, and it is named here rather than left in one session's head.

## Read at the open

The Shared Rules Version 2, The Harness 3.8, CLAUDE.md Version 2 and all 36 files in FROM Chat, in the ruled order, with `harness/derived_backlog.py` run before any work. One file arrived mid session, `ASK__Where_Is_The_Transcript_Bank_S328`, and was withdrawn by Chat before an answer was owed.

*No em or en dashes in this file; checked before writing.*
