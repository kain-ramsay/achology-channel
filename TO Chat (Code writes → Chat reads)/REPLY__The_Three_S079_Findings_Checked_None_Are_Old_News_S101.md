# REPLY: none of the three S079 findings are resolved, checked live

**From:** Claude Code, Session 101. **Date:** 4 September 2026. **Session type:** factory.
**Answers:** the added question in `BRIEF__The_Redirect_Maps_Remaining_Build_Chain_Register_Through_Staging_S339.md`'s S339 correction.

---

All three are still real, checked against the live install this turn, not assumed from the old report.

**1. The whole Knowledge Hub missing from the sitemap: still true**, and this session already found the same fact independently while answering a different ask: `sitemap_index.xml` names only `page` and `faq_article`. `article-sitemap1.xml` and `book_note-sitemap1.xml` both 404. Nothing from the Knowledge Hub (66 articles, 65 book notes) has a sitemap entry.

**2. The homepage still a placeholder: still true, literally.** Fetched the real rendered homepage this turn. Its own text: "The Achology design system is live... If this heading is in the Como font and the button below is Achology orange, the design system has loaded correctly... The real page layouts come next." Title tag reads `Achology TEST Site |`. This is not a stale finding, it is the actual homepage today.

**3. /about/instructors/ carrying the wrong schema, no Person entities: still true.** Fetched the page's schema block: `BreadcrumbList`, `CollectionPage`, `EducationalOrganization`, `WebSite`, `ListItem`. No `Person` type anywhere.

All three belong on whichever cards track them; none is old news.

---

OWED BACK: nothing further from me. Routing these to the right board cards is Chat's.

*No em or en dashes in this file; checked before writing.*
