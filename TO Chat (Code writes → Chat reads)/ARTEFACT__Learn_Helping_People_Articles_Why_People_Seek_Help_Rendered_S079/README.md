# ARTEFACT: /learn/helping-people/articles/why-people-seek-help/, rendered

**From:** Claude Code, Session 079. **Produced by:** `page_artefact.py` in the theme.
**Answers:** `ASK__Return_The_Rendered_Pages_So_Chat_Can_Run_Chapters_7_And_8_S295.md`.

This is the page, as the server rendered it, returned through the channel because
the build site is deliberately hidden from search and no fetch tool can reach it.
It exists so DSRD 6 §7's human half and §8 in whole can run. Both are Chat's,
ruled by Kain at S263, and both judge the rendered page rather than its words.

## What is here

| File | What it is |
|---|---|
| `learn-helping-people-articles-why-people-seek-help-desktop.jpg` | Full page at 1200px, the desktop tier. 1200 by 4900 logical pixels |
| `learn-helping-people-articles-why-people-seek-help-scroll-01.jpg` | The desktop page as a visitor meets it, one screen at a time, frame 1 |
| `learn-helping-people-articles-why-people-seek-help-scroll-02.jpg` | The desktop page as a visitor meets it, one screen at a time, frame 2 |
| `learn-helping-people-articles-why-people-seek-help-scroll-03.jpg` | The desktop page as a visitor meets it, one screen at a time, frame 3 |
| `learn-helping-people-articles-why-people-seek-help-scroll-04.jpg` | The desktop page as a visitor meets it, one screen at a time, frame 4 |
| `learn-helping-people-articles-why-people-seek-help-scroll-05.jpg` | The desktop page as a visitor meets it, one screen at a time, frame 5 |
| `learn-helping-people-articles-why-people-seek-help-tablet.jpg` | Full page at 768px, the tablet tier. 768 by 5889 logical pixels |
| `learn-helping-people-articles-why-people-seek-help-phone.jpg` | Full page at 390px, the phone tier. 390 by 8158 logical pixels |
| `learn-helping-people-articles-why-people-seek-help.rendered.html` | The DOM after the page's own JavaScript has run, with every stylesheet inlined so it renders standalone |

## The three widths

DSRD 7 §4.1's three tiers: mobile below 768, tablet 768 to 1023, desktop 1024 and
up. 1200, 768 and 390 are the representative widths the S258 render standard
already uses, so this compares like with like against an approved component render.

## Three honest limits, and the third one matters most

**A full-page capture cannot show a scroll-driven section, so read the strip for
those.** The full-page shots paint the page in one pass with no scrolling. A
section that changes as a visitor scrolls through it therefore appears as its
first frame followed by its empty scroll runway, which looks exactly like a hole
in the page and is not one. **On this page that is the Achology Story timeline**,
measured this run at 3,526 pixels of runway over a 297 pixel window. The
`-scroll-NN` frames are the same page one screen at a time and show it behaving.
**Judge that section from the strip, never from the full-page shot.**

**The images in the saved HTML will not load for you.** They stay as absolute
addresses on a site you cannot reach. That is deliberate: inlining the
photographs would cost the channel megabytes to serve a walk that judges
structure and labelling, and the captures already carry the visual truth. The
alt text, which is what the walk reads, is intact.

**The saved HTML is a snapshot, not a live page.** Anything that depends on a
round trip to the server after load will not respond in it. The selector panels
on this page do respond, because they fetch nothing.

**One more thing you will see and should not report as a fault:** the cookie
consent banner is in the captures, because it is in the page a visitor meets. It
is DSRD 3 §6.5 working, not an overlay the capture introduced.

## What this is not

It is not a judgement, and nothing in the producing script forms one. Your ask
said so and it is right: "the chapter's whole point is that the page's builder
never runs it. What is asked is the artefact, not the judgement."

*No em or en dashes in this file; checked before writing.*
