DONE: the project folder is out of Documents and the autosave runs again. Answers RULING__The_Project_Folder_Moves_Out_Of_Documents_S095.md.

# REPORT: the project folder has moved, and the record autosave is alive

**From:** Claude Code, Session 096. **Date:** 2 September 2026.
**Answers:** the OWED BACK line of `RULING__The_Project_Folder_Moves_Out_Of_Documents_S095.md`, and the closing paragraph of `RULING__Nine_Answers_From_The_S332_Sitting_S332.md`.
**Board card:** Hosting and Go-Live.

---

## What was done

The folder moved from `~/Documents` to the home folder, keeping its exact name, as the ruling called it. It ran as one script rather than as a sequence, deliberately: the hook launcher holds the project's one real path, so a folder that had moved with a launcher still pointing at Documents would have made every enforcement hook fail loudly and blocked the very command that fixes it.

## What named the folder from outside, and what was actually found

The ruling named four. **There were six.** The two it did not name are Claude Code's own user-level permission files, one of which is a settings file the ruling did not know held the path in prose, and Claude Code's per-project state key. Both are now repointed. Nothing was found by accident: the folder was searched for every mention before anything moved.

Rewritten, with the count each: the hook launcher, one line. The autosave's launch agent, one line. Claude Code's user settings, one mention. Its user local settings, two. The project's own settings, five. The project's own local settings, one hundred and twelve. The per-project state key in `.claude.json`, one.

**The memory note folder was copied, not moved.** It is named after the project's path, so it does not follow the folder, and this live session was still writing its transcript into the old one. Sixty two notes are in the new folder. The old folder is left in place until the session closes and the closing note is written into both.

## The proof

**The autosave's status file now reads OK at 18:19 on 2 September**, written by the launch agent itself. Before the move it had been dead since 10:57, failing every hour with `Operation not permitted` reading its own script. The error file's last failure is timestamped before the move and there has been none since.

The strict form of the ruling's proof, an unaided hourly tick with nobody touching it, falls due around 19:19 and is being watched; the 18:19 write already proves the cause is gone, because the permission refusal did not depend on who started the job.

## What else came clean through it

Git is untouched: the repository is the folder, so its history and its remote travelled intact, and the working tree is clean at commit `9b56454`. The channel at `~/achology-channel` was never involved. The hooks resolve the harness at its new location and fire normally; H2, H6 and H7 all fired during this move and all three were right to.

The move also takes the project folder out of iCloud, which closes the question S094 raised and nobody had ruled on.

## The one thing left, and it is Kain's hands

Chat reads the project folder through the Filesystem connector, whose allowed folder is the old Documents path. **Kain repoints it at the new location before the next Chat session**, or Chat opens blind to every project file. He has been told in the sitting.

---

OWED BACK: nothing. The unaided hourly tick is reported in the S096 session report if it does not land before this file is read.

*No em or en dashes in this file; checked before writing.*
