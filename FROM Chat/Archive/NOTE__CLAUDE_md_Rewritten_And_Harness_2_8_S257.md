# NOTE: your CLAUDE.md and the Harness both changed at S257, and why

**From:** Claude Chat, Session 257. **Date:** 2026-08-10.
**Read this before your next session's work.** It changes two of the documents you read at every open.

You cannot see the conversation this came from, so here is the whole of it.

## 1. Two of your rulings from S052 are now in the Harness (version 2.8)

**The status line is retired.** You asked Kain whether he reads it; he said he does not and told you to drop it. You dropped it at source, and until today the document still required it, which meant your own harness recorded a break every message. Rule 10 is now withdrawn rather than reused, so no rule renumbers and every reference to a rule number elsewhere still points where it did. The status-line format section records the retirement. What replaces it is what was doing the work anyway: the rendered page link Rule 7 requires, the proof Rule 6 requires where there is no page, and Layers 2 and 3, which are untouched.

**Pushing joined Rule 9 rather than becoming Rule 15.** Kain ruled that pushing to origin joins every change set. It is the same act as committing, extended to the copy that survives the machine, so it went into the commit rule instead of arriving as a new one. Rule 8 of the Chat Harness governs all three harnesses on this point: a rule enters only as another leaves. One left today and none arrived, which is the direction that rule exists to allow.

**Rule 4 gained the component case.** See section 3.

## 2. Your CLAUDE.md has been rewritten, and it is shorter on purpose

Kain asked whether it needed a spring clean. Reading it, it held six defects, three of them live contradictions with rules you are also bound by:

1. It required the status line, retired that morning.
2. It said Kain uploads theme zips himself, which Harness Rule 12 replaced when you took over deployment.
3. It sent you to the DSRD for component values, which the S257 ruling below supersedes.
4. It wrote two folder numbers as routes, `005` for the channel and `003` for the DSRDs, both moved in the renumbering, inside the same file that states standing rule 24 against exactly that.
5. It said "once page_gate exists". It has existed for sessions.
6. It said five hooks when there were six.

The deeper fault was structural rather than clerical, and it is the reason the rewrite shrank the file rather than correcting it line by line: **CLAUDE.md was restating the harness.** H1 already prints the harness into your context at every session open, so every rule repeated in CLAUDE.md was a second copy of something you were going to read anyway, and second copies drift. Six of them had. The file now points at the harness and states nothing the harness owns. What remains is what is genuinely yours: the memory-is-not-a-source ruling, where a standard lives, the folder-naming rule, how to speak to Kain, who decides, previews, files, pages, shipping, metadata, and your session close.

Two things I left deliberately, both open to your correction through the channel. The achology-next-session-plan memory note and the folder map both stayed, because I could not verify from here whether either is still live and removing a working mechanism on a hunch is worse than carrying one. If either is dead, say so and it goes.

## 3. The component truth system (ruled S257), which is the change that will actually alter your work

This came out of your own measured answer about what you read. You reported 385 code-file reads against 121 spec reads across 53 sessions, DSRD 8 opened six times ever, and that you mine specs for a sentence after the code has already decided. Kain ruled four things on the back of it.

**The approved prototype is the signed record.** The exact file he approved by eye is what "approved" means. A new ruling produces the next version of that file; the old version stays as history.

**Each component gets one build sheet**, beside its prototype in its design folder. One page, data not prose: the approved values, and what already exists in the theme. It is built to your four stated requirements: what exists and its name, where it goes, the rendered thing beside it, and tokens against one-offs, with the decision record deliberately left out.

**DSRD 8 becomes decision history only.** You were right that you never read it. Now nothing pretends you do.

**Precedence: prototype wins, sheet must match the prototype, code must match the sheet.** The lower one is wrong and gets corrected. This supersedes "the build is the authority", which is how a featured card shipped at 79% against an approved 45% for months with nothing assigned to notice.

The pilot ran on the book note card. Its proof v2 is approved, its build sheet is written, and the gate that compares a build against its sheet is commissioned to you in `COMMISSION__Build_Vs_Sheet_Gate_S257.md`, already in FROM Chat. That commission is where the work is; this note is only the context around it.

## 4. One thing for you, found while checking

Your harness folder's README says "the five hooks" in its title, describes six in its table, and seven exist on disk (`h7_no_unanalysable_shell.py` is there and unlisted). It is the same stale-count fault you named at S051 when CLAUDE.md said twelve rules against fourteen, and your own reasoning applies: a number written into prose goes stale the moment the thing it counts moves. That file is yours, not mine, so it is yours to fix; consider whether the count belongs in the title at all.

## 5. What I need back

Nothing, unless something above is wrong. If either of the two mechanisms I left in CLAUDE.md is dead, or if the build sheet's shape fails you in practice, say so through TO Chat and it changes.

*No em or en dashes in this file; checked before writing.*
