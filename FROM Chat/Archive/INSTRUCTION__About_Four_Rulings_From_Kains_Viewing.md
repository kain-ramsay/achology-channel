# INSTRUCTION: four rulings from Kain's viewing of the About page (v0.37.0)

**From:** Claude Chat, S238. **Date:** 2026-08-04. **Ruled by Kain in session, from the rendered page.**
**Context:** Kain is viewing the live About page on v0.37.0 (he quotes the page-updated line, which exists only in your S044 build, so the upload has happened). These four rulings amend the signed spec and its addenda; once the build is approved by his eye, Chat reconciles the spec to the build, as with the Policies index.

## 1. Remove the visible page-updated line

Kain's ruling: a visible "page updated" date is inappropriate on a main feature page. Remove the line. The schema keeps datePublished and dateModified exactly as you wired them; only the visible line goes. DSRD 6 §12's About row now exempts §6's visible date as well as the author line (edited this session on Chat's side), so the gate re-verdicts that signal without failing the page.

## 2. Bring every button on the page to the DSRD 7 §5.1 system

Kain's ruling: the page's buttons are not standardised to the house system. Audit every button on /about/ against DSRD 7 §5.1 and bring each to the system. The new pairing rule, written into §5.1 this session: buttons are uniform, and where two sit side by side as a pair they contrast, one primary solid and one secondary or ghost, never two of the same style, so the pair reads as a main action and an alternative. Page-local fixes ride this change set; any button whose style lives in a shared file stops and reports per Rule 3.

## 3. The hairline beneath the hero

Kain's ruling: at desktop, the hairline under the hero banner is too wide. It aligns to the edges of the content block inside the hero, not the full page width. At tablet and phone it stretches to the full width as now. Build it and return the rendered page for his eye.

## 4. The phone hero treatment

Kain's ruling: at phone width, the banner image stops being a foreground feature. It moves behind the hero text as a very transparent backdrop, so the text leads and the page gets shorter; mobile visitors reach the information, not a large image that says nothing on its own. Desktop and tablet keep the image exactly as built. Build a first treatment (the opacity and positioning values are yours to propose on the render), return the rendered page at phone width, and iterate on Kain's eye.

## Definition of done

All four shipped as one change set under the harness, the zip rebuilt for Kain, the rendered page returned through TO Chat for his viewing at desktop and phone, and the DSRD 6 record refreshed after his word. Where any of the four touches a shared file, stop on that item and report; ship the rest.

*No em or en dashes in this file; checked before writing.*
