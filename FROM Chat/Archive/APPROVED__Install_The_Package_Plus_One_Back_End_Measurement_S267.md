# APPROVED: install the package. And one measurement, before two board cards can be set honestly

**DOCUMENT TYPE:** not a page spec.
**From:** Claude Chat, Session 267. **Date:** 2026-08-12.
**Authority for item 1:** Kain, in session, asked directly and answered directly.

## 1. Yes. Install it

**Kain's answer: yes.** Install what the accessibility scan and the desktop browser check need, and download the browser binaries with it.

That unblocks two of the eight machine checks: the accessibility scan that gives chapter 7 its floor, and the browser and device check in chapter 11. Report each one's acceptance printout in the usual shape, including the case where it is made to say no on an answer you already know.

Rule 11 stands unchanged for everything else: this is a tool on your machine for measuring, not outside code entering the theme, and anything that would go into the theme is still a separate question to him every time.

## 2. A measurement, because two board cards cannot be set from inference

Kain asked two questions at the close of S267 and I could not answer either honestly, because both turn on what actually exists on the site rather than on what any document says.

**Card one: "Plugins and Site Configuration."** It reads To Do. I can see that at least two of its parts are delivered: the cookie consent mechanism is built and verified with the footer control the Cookie Policy promises, and Rank Math is live and doing real work, since it generates the sitemap and holds the titles, descriptions and keyphrases across the help section. I cannot see the rest.

**Card two: "Build WordPress Back End."** It also reads To Do, and that is plainly wrong: the help articles, the reviews and the book note template are all live, which means post types, taxonomies and templates exist. What I cannot see is how much of its definition of done is met and how much is not.

**Setting either card from what I can infer would put a guess on the board, which is the exact failure your Part 5 answer named.** So the question is a measurement, not a judgement.

### What I need, read from the site and the theme rather than from any document

1. **Post types.** Which are registered, and how many published items each holds.
2. **Taxonomies.** Which are registered, against which post types, and how many terms each holds.
3. **Field groups.** Which exist, whether they are held in version-controlled files, and which post types they attach to.
4. **Plugins.** Every plugin active on the build site, with its version, and whether each is configured or merely installed where you can tell the difference.
5. **The four legacy suites**, by name: are any of the old-site plugins still present, active or inactive.
6. **Analytics and tracking.** Whether the analytics container and tag manager are present, and whether any of the ten named events fire.
7. **Search.** Whether a search plugin is installed and whether it indexes the content types.

**Where a thing exists but is unconfigured, say so in those words**, because the difference between installed and configured is the whole difference between the two cards being nearly done and barely started.

No opinion needed on either card. The counts settle them.

## 3. The `/cards/` page: it stays, and it must not be publicly reachable. The mechanism is yours

Kain ruled at the close of S267: **the page stays, and it should not be published.** He is right on both halves, and the second half is the one that needs your answer rather than mine.

**Why it stays.** Your build against sheet report names `/cards/` as where the quote, article and workbook cards render, which makes it the specimen page five build sheets need before the gate can measure them. Right now that gate measures one component out of eleven. Taking the page down would blind the only instrument that checks whether the built cards match what Kain approved, at the moment we are trying to widen it from one to six.

**Why it should not be published.** It is a workbench. It carries no reader's job, it is named in no document, and it exists to be measured. A published page that nobody decided to publish is exactly the kind of thing that reaches the public at cutover because nobody was assigned to notice it.

**The requirement, stated as an outcome:** `/cards/` is never reachable by a member of the public, at cutover or after, and the component gate can still read it. **How that is achieved is your call, because it turns on a fact only you hold:** whether your gate can fetch an unpublished page at all. If it can, unpublish it and say so. If it cannot, the page stays published and carries whatever keeps it out of the public's way, and you name what that is and prove it, rather than either of us assuming the site-wide noindex will still be there after cutover. It will not be: the whole point of cutover is turning that setting off.

**Either way, report what you did and what proves it.** This is not a page for Kain to look at; it is a page that must never be found.

*No em or en dashes in this file; checked before writing.*
