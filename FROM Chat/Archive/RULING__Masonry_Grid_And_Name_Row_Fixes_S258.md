# RULING and INSTRUCTION: Reviews archive grid is masonry; render two name-row fixes (S258)

**From:** Claude Chat, Session 258. **Date:** 2026-08-10.
**Authority:** Kain, directly in the Chat session, responding to your DELIVERY__The_Reviews_Archive_S053 sections 6 and its rendered question.

## 1. The card height question is settled: masonry

Kain rules neither of your two rendered options. He has agreed masonry with you already and confirms it here: a masonry grid, so each card fits its own review and cards stack directly under one another per column, no empty space in any card. Three columns at desktop, two at tablet, one at phone, as the plan already says. Build it, and delete both temporary switches (`rv_fit` and the stretched default) once masonry is in, per your own annotation that the loser dies on ruling.

## 2. The name row: a problem Kain has named, two fixes to render for his eye

On the live page Kain sees reviewer names like "Cathy K." wrapping onto two lines in the attribution row at three across. His words: we cannot have names over onto two lines, it looks really bad.

Two candidate fixes were discussed. Render BOTH on the live page behind temporary switches, same mechanism you used for the height question, so he can pick in Safari:

- **Fix A: abbreviate the month** in the date, so the row is shorter and the name has more room on the one line.
- **Fix B: the date moves directly underneath the name**, two quiet lines instead of one row, so the name owns its own line and can never wrap against the date.

Chat's steer, which Kain has seen: Fix B, because a shortened month still wraps on a long enough name, while a name on its own line cannot. But this is his eye's call, per the S258 render standard: both fixes on the same real cards, one variable changed, and he rules on the rendered page.

Render both with masonry in place, since masonry changes the card widths' behaviour and the ruling must be made on the layout that will ship.

## 3. Still open from your delivery, not ruled here

The capitals question ("OLEKSANDR D.") is not yet ruled; leave names exactly as typed until it is. The Notion AI pass (theme tags and titles), the Featured flags for the standouts block, the schema question, and the three archive copy slots are all with Chat and Kain and will come to you as rulings or briefs through this channel.

*No em or en dashes in this file; checked before writing.*
