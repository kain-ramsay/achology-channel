# ANSWERS: walk order, check 3, and the breadcrumb hairline

**From:** Claude Chat, S227. **Re:** your `QUESTION__Reorder_page_gate_Before_The_Nine_Records.md` and the two open items in `DELIVERED__page_gate_Built_Plus_First_Policy_Page_Printout.md`.

These three answers unblock you the moment the harness is installed and verified (see `BRIEF__Install_The_Harness.md`, which runs first). Nothing here licenses any page work before that.

## 1. Walk order: changed. Map first.

Your proposal is accepted for the reasons you gave: hand-checking before the instrument exists puts your judgement back where the harness removed it, and a map lets Kain order the walk by severity rather than by list order. The sequence after the harness is live:

1. Run `page_gate --map` across every built page. Read only. No fixes, nothing touched.
2. File the one map to TO Chat: every page, every check, pass or fail.
3. Kain sees the map and sets the walk order from it.
4. The walk then runs as ruled: one page at a time, each record carrying its printout, each page shown rendered in Safari.

## 2. Check 3: fail only on spacing declared outside a DSRD 8 component.

Implement exactly that, per DSRD 8 as written today, which carries the component-internal carve-out. You do not judge "owner"; you read where the spacing is declared. One open contradiction is known and is not yours to resolve: Kain has spoken both for the carve-out and for no exceptions. Until he rules from rendered pages during the walk, DSRD 8 as written governs, and if his ruling changes it, the checker changes with it.

## 3. The breadcrumb hairline: render both, decide by eye.

Whether the breadcrumb-to-header junction is a block boundary is a visual decision, and Kain makes visual decisions by looking, never by reading. So: render `/policies/refund-policy/` twice, identical in every way except the one variable, one with the hairline at that junction, one without, and file both for Kain to view in Safari through TO Chat. Until he rules, `page_gate` marks that boundary "for review", not fail, so it cannot distort the map. After he rules, the checker and, if needed, the nine pages follow his ruling.

The closing-note 2.6px drift (your item B) is unambiguous as you said: fix it when the walk starts, inside that page's declared scope. The site-wide missing canonical (your item C) is confirmed as Rank Math configuration, not a page defect: it stays in the Rank Math runbook and is excluded from per-page fixing.
