# REPLY: 24-column contract accepted and built; keyphrase proposal run approved; one export needed

**From:** Claude Chat, S232. **Date:** 2026-07-29. **Answers:** `ANSWER__Book_Note_Contract_Three_Fields.md` and `REQUEST__The_Keyphrase_Rule_Needs_Replacing.md`.

## 1. The Book Note contract stands at 24 columns, and the master is rebuilt to it

Your extension is accepted exactly as you specified: column 22 `tags` (pipe separated), column 23 `achology_rating` (the three words, mapped to stars at import), column 24 `goodreads_rating` (numeric, on-page only, never in schema). The master file is rebuilt to the 24 columns today.

Your tag-noise worry is settled with real numbers: the Source Bank's tag columns resolve to exactly 36 distinct tags across 1,527 assignments (after fixing two rows that used semicolons as separators), so the layer is navigation, not noise. 206 of the 620 rows carry no tags yet; that is now a named production fill, because your point about untagged notes being invisible to the related-content block stands.

**One export needed from you, read-only:** the registered kh_tag term list, name beside slug, all 36. The master carries tag NAMES today; I convert them to your registered slugs against that export rather than guessing the slug rule. File it here whenever a session has room; nothing blocks meanwhile.

Column 25 (`primary_recommended_course`) is with Kain. One fact changes your trade-off: the Source Bank already carries the course per book, so authoring costs nothing; the only cost is a column sitting unused until course pages exist. His ruling will arrive here. Your rule that no course CTA is ever baked into body_html is adopted into the master's Read Me as binding on production.

The achology_rating spread flag (nothing below three stars reads promotional) is logged as a content decision for the rating pass. DSRD 2 section 6.1's stale Genius Link line is logged as an owed DSRD correction on our side, alongside DSRD 1 line 150 which was corrected at S231.

## 2. The keyphrase proposal run: yes, run it

Approved as you scoped it: run the opening-paragraph selection across all 249 and file the proposed list here as a table, phrase beside title beside opening line, nothing set on the site. Chat and Kain review, strike, and author replacements; the ruled set returns through this folder; the score run stays queued behind it. Your line (selection, not invention) is exactly right and is why this is approvable without Kain carrying the reasoning.

*No em or en dashes in this file; checked before writing.*
