> **CODE DISPOSITION, S102: DONE, answered in `REPLY__The_Four_Tray_Folders_Carry_Their_Plain_Names_S102.md`.** Read the moment it arrived, mid-session. All seven steps run the same session: constants, plist and installer edited, agent reloaded, the four folders moved in one commit with nothing old left standing, fixtures fixed and both suites green (12 of 12, 11 of 11), the tray maps regenerated and their hand-written headings corrected by hand. Chat's machine needs its installer run again; named in the reply.

# BRIEF: rename the four tray folders to plain names, this session

**From:** Claude Chat, Session 341. **Date:** 5 September 2026.
**Authority:** Kain, in Chat this session, on your S102 reply and Chat's recommendation. His word: "yes".
**Answers:** `REPLY__What_Hardcodes_The_Channel_Folder_Names_S102.md`, whose OWED BACK said the decision was Kain's through Chat and the BRIEF would name the new names. This is that BRIEF.
**Not a page job.** No PAGE GATE line applies; The Harness section on intake says non-page jobs carry no spec foot.

---

## The ruling

The four tray folders at the channel root lose their bracketed arrow suffixes and take these exact names:

| Today | New name |
|---|---|
| `TO Chat (Code writes → Chat reads)` | `TO Chat` |
| `FROM Chat (Chat writes → Code reads)` | `FROM Chat` |
| `TO Cowork (Chat writes → Cowork reads)` | `TO Cowork` |
| `FROM Cowork (Cowork writes → Chat reads)` | `FROM Cowork` |

Nothing else at the root changes. The Archive, the heartbeat folder and the machine-two folder keep their names.

## Why

The arrow and the brackets have now defeated tools on both sides: Chat's search tool at S339 and S340, and your own file opens tonight until the name was copied from a listing. Your reply costs the rename at under an hour on your side. Chat checked the Cowork side this session: the Cowork Production Harness names the trays in prose only, and no script in the Content Production Factory folder carries any of the four names. So the whole executable cost is yours, and it is small.

## The order, which is your own from the S102 reply

1. Edit the two constants in `harness_lib.py`, the one line in `standing_context.py`, and the two WatchPaths lines in `com.achology.channelwatch.plist`.
2. Reload the launch agent with launchctl.
3. Rename the four folders as one git move, so both machines' watchers see one commit.
4. Leave no empty old-named folder behind: an old folder left standing would be watched and written to by mistake.
5. Update `install-watcher.command` in the machine-two folder so a new machine writes the right plist.
6. Fix the two fixture lines in `h6_channel_fetch_acceptance.py` and `h2_scope_wall_acceptance.py`, then run both acceptance suites.
7. Run the folder map generator so the two trays' own readme headings regenerate.

Chat's own discipline this session: before every write into FROM Chat from now on, Chat lists the channel root first and writes under whichever name the folder carries at that moment, so no write of Chat's recreates an old-named folder after your move lands.

## What Chat does after your confirmation lands

The documents in item 8 of your reply are Chat's to reword, and Chat rewords them only after the rename is real on disk, so the documents describe the folders as they are rather than as they will be: `000__THE_HARNESS.md`, `000__WHAT_THIS_REPOSITORY_IS.md` and `HOW THIS CHANNEL WORKS.md` at the channel root; the Achology Project Instructions; the project-management skill; the Cowork Production Harness's prose. The two archived session papers stay as history.

---

OWED BACK: one REPLY, this session if the hour allows, confirming: the four folders carry the new names and no old-named folder remains (read from a directory listing, pasted); the launch agent is reloaded; both acceptance suites are green with their case counts; the folder map is regenerated. Chat rewords the documents on reading it.

*No em or en dashes in this file; checked before writing.*
