# RULING: leave page 33498 in place, rename its title, and the CLAUDE.md line is fixed

**DOCUMENT TYPE:** ruling, not a page spec. No PAGE GATE line applies.
**From:** Claude Chat, Session 274, 15 August 2026.
**Answers:** `REPORT__Side_Tabs_Built_The_Academy_Deletion_Refused_S059.md`, and the `CLAUDE.md` contradiction raised in your folder map reply.

---

## 1. The `/academy/` page: your recommendation is ruled, by Kain, this session

**Page 33498 stays exactly where it is.** Kain saw your evidence and ruled with it: the deletion is withdrawn, not deferred.

His ruling was originally given on a description of a stray duplicate secondary page. What you found is the structural parent giving all 35 school and course pages their `/academy/` addresses, whose removal would rewrite every one of them, break DSRD 1 section 2.3, and orphan the 76 in-body links you counted. That is new information, and stopping to surface it rather than executing or working around it was correct.

**Do this instead, and only this.** Rename its title from "The Academy of Modern Applied Psychology" to something that reads plainly as a structural placeholder rather than a real page. Your call on the exact wording, since you can see how it presents in the Pages list; something on the lines of "Academy (structural parent, do not publish)" is the shape. It stays a draft.

**What happens later, so nobody re-derives this.** When the real `/academy/` landing page is built from its own signed spec, either 33498 is promoted into that page or the seven schools are re-parented onto the new one, in the same sitting, never across two. Both routes are open; the choice belongs to that page's spec session, not to this one.

## 2. The `CLAUDE.md` contradiction: fixed

You were right to name it rather than edit it, and Kain has ruled the change. The session-close instruction that commanded a hand-maintained root folder map now commands the generator instead, points at `SPEC__Folder_Navigation_And_Map_Currency_S274.md` as the governing standard, and records that the old root map was retired to a tombstone at S274 after going stale and contradicting the live project map.

The reasoning is written into the instruction rather than left implied: a folder map's purpose half is written by a person once, and its contents half is generated, so editing one from memory is not merely discouraged, it cannot be done correctly.

## 3. The video rename map

Your blocker is with Kain as an action for him and Karen. Nothing for you until the Drive is mounted and the 28 course folders are readable. It is recorded as blocked rather than silent, which was the half of that failure worth fixing.

---

Everything else from your four reports is answered in `REPLY__Four_Reports_Answered_And_What_Is_Settled_S274.md`, written to this folder in the same sitting.

*No em or en dashes in this file; checked before writing.*
