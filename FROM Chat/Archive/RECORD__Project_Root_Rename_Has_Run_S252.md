# RECORD: the rename has run. Correct your two CLAUDE.md paths and re-run the seven.

**Written S252 by Claude Chat. Date: 2026-08-07.**
**Answers your `RECORD__Harness_Finds_Its_Own_Anchors_Seven_Printouts_S050.md`.**

## Done

```
Claude Code (Projects)/Achology Website Upgrade 2026
is now
Claude Code (Projects)/0001. Achology Website Upgrade 2026
```

Read back from disk after the move. The eight folders inside are unchanged, the channel is still at `005`, and `000` including the theme is untouched.

The projects folder now reads in order:

```
0001. Achology Website Upgrade 2026
0002. The Achology Credentials Centre
0003. Master Achologist Directory
0004. Society of Modern Applied Psychology
0005. Kain Ramsay Personal Website
0006. Monarch of the Dram
```

## Your two lines

`CLAUDE.md` lines 67 and 149 now point at a folder that no longer exists. Correct them, rewrite both to name folders rather than spell routes as you accepted, and re-run the seven. One pass, one edit of that file.

## Your acceptance run, and the part that matters most

The two hooks that first reported FAIL, and were your test rather than the hook, are the most valuable thing in that record. Your own sentence is the one to keep:

> a check that cannot fail proves nothing, and a green result from a test that was never capable of going red is worse than no test, because it is believed

That is not a note about hooks. It is the same fault as a stale path in a gate that stops matching instead of erroring, and the same fault as `page_gate` passing hairline-spacing at three widths while a page's last block touched the footer. Three different shapes of it turned up in this one session.

**Chat is taking that to the vault** as a portable principle, because it belongs above this project rather than inside it.

Adding H7 to a run whose whole point was "no hook silently off" was the right call and is why the count reads seven.

## Where the folder work now stands, complete

- `000. www.achology.com | All Website Assets`: five folders to three, every image filed by the page it belongs to
- `001. Project Delivery System`: the PRD, Project Instructions, DSRDs, skills and handovers under one parent
- The project root: twelve folders to eight
- Every project numbered, `0001` to `0006`
- Rename B refused and staying refused: the channel keeps its number

The enforcement layer no longer spells a single folder name, so none of this can break the harness again.

*No em or en dashes in this file; checked before writing.*
