> **CODE DISPOSITION, S110: WAITS ON a session with room for it, and nothing else.** Arrived mid-session and read in full under the channel wall, on the turn it landed; it cancels nothing in the sitting it arrived in, which is Kain's own work on the help and article templates and has run its whole length. **Nothing blocks this brief:** it is signed, it names its 200 files by prefix, the records have each passed content_gate individually, and Kain's word authorising it is recorded. It is not started because a 200-page import, score and route pass is a session's work, not a corner of one, and starting it late would leave 200 drafts half scored. It is named first in the next-session prompt. **One item in it is cheap and is answered there rather than left to the pass:** section 3's protocol question, whether the theme adds `https://` to a bare `community.achology.com/checkout/...` or emits it as-is, is read from the theme in minutes and decides whether every rendered checkout button from that field is broken; it is worth answering before 200 pages are built on the field either way. Testable when the session report with the score table and the protocol answer reaches TO Chat.

# BRIEF: import and score all 200 course 018 quote records as drafts, route every failure to its owner, publish nothing

**DOCUMENT TYPE:** brief, from Claude Chat, Session 356. **Date:** Thursday 10 September 2026.
**Authority:** Kain, live in the Cowork sitting of 10 September, in his own words ("YES ... please go ahead"), recorded by Cowork in `FROM Cowork/RULING__Course_018_200_Of_200_Reached_Kain_Approves_Commissioning_Codes_Import_And_Score_Pass.md`; signed here by Chat as the brief that word authorises. Cowork drafted the content below; Chat confirmed it against `rank-math-90` Part B and forwards it whole with one addition (section 3).
**Board card:** Course quotes: 200 quote pages from course 018, proved on the first twenty (3d64da19af3581fdae6fc83efaff6e1c).
**Read this cold.**

---

## 1. Scope

All 200 files in `Content Records/quote-page/` whose name starts with `CQ018-` and ends in `.md`. Cowork counted exactly 200 on device this session. The same folder holds the book-derived quote pages (`Q0####__...`) and the run's Batch_Report documents; neither is in scope, and the `CQ018-` filter excludes both. Every one of the 200 has passed `content_gate.py` individually and carries the S356 shape: title-case H1, the opening paragraph with the quote inside it, Kain's provenance formula naming the lecture, the three fixed headings, the closing question inside its band, the `practice` field, and the link labels to the S356 standard.

## 2. The work, in `rank-math-90` Part B's own order

1. **Front door.** Run the front door checks on all 200: fields complete, body carrying only its ruled sections, every named file present. A record that fails does not import; list it in the session report rather than stopping the batch.
2. **Import as drafts.** Import every record that passes, as draft. Regenerate the sheet from the records. Read every page back off the install.
3. **Score.** Read the Rank Math score off each page. Write it, with the keyword and every failing test named, into the score table in your session report, and the score line into the page's DSRD 6 record. That is the only place a score is ever written.
4. **Route every failure to its owner.** A body fault goes back to the record, named in the report, for Cowork to redraft. A template fault (a table of contents not detected, alt text not landing, a slot missing, anything the theme's rather than the record's) is yours. No body is fixed on the install.
5. **Publish nothing.** Publishing, live status and the live date wait on Kain's own word, given after his Safari sitting on a sample of the imported pages. That word has not been given and is not in this brief.

## 3. One thing to read before the import, added by Chat

Every CQ018 record's `destination_course_url` and `source_book_note_address` carry the DSRD 4 checkout address as a bare domain with no protocol (`community.achology.com/checkout/...`). Cowork found it and asked, in `FROM Cowork/ASK__Does_The_Checkout_Link_Field_Get_Its_Protocol_Added_By_The_Theme_S356.md`, whether the theme adds `https://` when it builds a button from that field or emits the value as-is. **Answer it in this pass, read from the theme, not guessed:** if the theme emits it as-is, every rendered checkout button from that field is broken, and the fix is at the source (DSRD 4's table, Chat's) or in the rendering (yours, defensively), never in 200 record bodies. Say which in one line, and say how many published pages of any type read that field today.

## 4. Acceptance criteria

All 200 records through the front door. Every imported record carries a read Rank Math score in your session report and in its own DSRD 6 record. Every failure named with its cause and routed to its owner. Nothing published. The protocol question answered. The session report reaches Chat through TO Chat.

## 5. What this brief does not authorise

Publishing any page. Fixing a body fault on the install rather than at the record. Treating a good score as the card's Definition of Done, which still needs Kain's Safari sitting and the DSRD 6 record per page after this pass.

OWED BACK: the session report with the score table and the protocol answer. Nothing else.

*No em or en dashes in this file; checked before writing.*
