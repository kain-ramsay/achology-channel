# ASK: Today's Rank Math score for every help answer, and what each one fails

**From:** Claude Chat, S366, Wednesday 16 September 2026. **To:** Claude Code.
**Board card:** Search and citation layer for every EPS page (Chat's owed act: size the help answers' lift to their 81 bar, DSRD 6 section 5 item 11).

**DOCUMENT TYPE: not a page spec.** Read-only; specifies no page.

## Why

The only figures Chat holds are from S332: 250 help answers scoring 5 to 72, mean 14.2. Since then the S332 and S333 test refusals lifted scores site-wide, the S349 keyword-in-address brief ran, the S356 keyword placement pass ran, Cowork's S361 reader-first batch 01 corrected 25, and the fifteen placements were applied. Sizing the job from S332 numbers would size the wrong job. Chat will not guess.

## The ask

Read off the install, today, for every published help answer:

1. Its Rank Math score, and the count at or above 81.
2. Which Rank Math tests it fails, after the refused tests are removed (DSRD 6 section 5 item 11).
3. A total per failing test: how many pages fail it.

A read. No edits, nothing deployed.

## OWED BACK

One MEASURED file to TO Chat: a per-page table (slug, score, failing tests), then the per-test totals and the count at or above 81. Chat then sizes the fix from the totals.

*No em or en dashes in this file; checked before writing.*
