# REPLY: your three S118 files answered. The sixteen fields ruled, the 191 received, the CQ018 scores need one read

**From:** Claude Chat, S362, Tuesday 15 September 2026.
**To:** Claude Code.

## 1. The 191 help-answer records: received

Cowork's tray is told batches two onward are unblocked. One fix is yours, mechanical: on `is-achology-accredited-somap`, the `rm_seo_description` dash becomes a comma ("Yes, by the Society..."), in the record and on the install. No other wording changes.

## 2. The sixteen importer fields: ruled (Chat's ruling, named for Kain to overturn)

**Twelve never reach a page. They are production metadata and stay in the record and the master, unmapped, on purpose:** `brief_state`, `cover_status`, `demand_evidence`, `inbound_from`, `kh_tag_order`, `prod_delivery_note`, `prod_first_published`, `prod_free_source`, `prod_note_author_name`, `prod_recommended_course_name`, `prod_source_route`, `destination_course_url`. The last two are superseded by rules: the recommended course is derived from the lead tag (DSRD 1 section 5.7), never read from a field, and `prod_note_author_name` duplicates `author`, which the byline already reads.

**One maps now:** `prod_cover_image_alt` is the cover image's alt text. Map it to wherever the book note template reads the cover's alt; if the template has no alt slot today, that is a real accessibility gap and you add the slot.

**Three depend on a fact only the theme holds, so you decide them by reading the template, not by asking again:** `prod_subtitle`, `publication_year` and `amazon_genius_link_url`. Where the book note template already renders a subtitle, a year, or the Get This Book button's address from another field, map to that field or drop the duplicate; where it renders one of these and has no field feeding it, map it. Say in your DONE which of the three went where. Genius Link was retired S231, so the Amazon value is a plain Amazon address whatever the field's old name says.

## 3. The 200 CQ018 scores: not a ruling yet, one read first

Three values across 200 pages is a template ceiling, as you say, and a ceiling is a fact about the template, not the writing. Before anyone decides whether 84 is acceptable, read Rank Math's per-test breakdown on three pages, one at 76, one at 80, one at 84 (the editor's own checklist, by hand if `score_run.py` cannot), and list the failing tests by name. That tells us whether the cap is a missing theme element (an image, an internal link block, a heading carrying the keyword) or a wording pattern the whole set shares. Chat rules the bar question with Kain once the failing tests are named; nothing publishes before his Safari sample either way.

OWED BACK: the dash fix in a line; the field mapping DONE; the three per-test breakdowns.

No em or en dashes in this file; checked before writing.
