> **CODE DISPOSITION, S103: WAITS ON** the next factory session. Read in full the moment it arrived, mid-session. It is not theme work and it says so in its own harness line: a tooling and asset pass with no template, stylesheet or page look change, which Harness Rule 1's split sends to the factory session. S103 is a theme session on Kain's pick, clearing `000__THE_THEME_QUEUE.md`, and nothing in this brief cancels or touches that work. Testable when `Page Images` holds 117 files named `{slug}.png` and the map's rows 2 to 118 each match one.

# BRIEF: Kain's 117 salvage hero masters are on disk; rename them from Canva's page names and run the whole set through the pipeline

**From:** Claude Chat, Session 343, 5 September 2026, late.
**Commissioned by:** Kain, in session, having made and exported all 118 Canva pages himself tonight.
**Runs:** `BRIEF__The_77_Rescued_Article_Hero_Images_From_Canva_Export_To_Live_Page_S340.md`, steps 1 to 5, over the complete set rather than the first 83.
**Board cards:** the rescued field-authority articles; Image and icon optimisation.
**Harness:** The Harness applies unchanged. This is a tooling and asset pass; no template, stylesheet or page look changes, so no ruling and no Safari sitting.

## 1. Where they are, and what state they are in

Kain exported the whole Canva design in one download and unzipped it in the Article Page folder's `Page Images`. The masters sit in a subfolder named after the design, `SALVAGE Articles from the Old Website for the New`, alongside the original `.zip`. **118 PNG files, about 261MB in total, roughly 2.2MB each.**

They are a fresh export of the entire set, not an addition to it: **Kain revised a number of the pictures he made yesterday**, so every file here supersedes anything of the same subject that was in `Page Images` before. Treat this folder as the whole truth for salvage hero masters and re-run the pipeline over all of them, not only the new ones. The previous loose masters are gone from `Page Images`; the zip and this folder are what remain.

## 2. The one fault in every filename

Canva named each export after its page name, and the page names carry the Magic Media prompt words as well as the slug. So every file reads:

`{slug} _ key_ {key words}.png`

for example `a-guide-to-building-inner-resilience _ key_ bending not breaking, reed.png`.

**The rename rule is exact and mechanical: keep everything before the first occurrence of ` _ key_ ` and append `.png`.** That yields `{slug}.png`, which is what step 1 of the S340 brief expects. Do it in a script, not by hand, and print the before-and-after table.

## 3. Two files that will not match the rule, and what each is

**One is not a salvage article and takes no picture.** `Why People Seek Help, and What They Actually Need From It.png` is page 1 of the design, the instructor-article page the whole file was copied from. `ARTICLE_HERO_IMAGE_MAP_S340.csv` row 1 says so in its own words: "(ignore page 1: it is the instructor article image the file was copied from)". **Discard it from the run.** That leaves 117 masters for the 117 real rows, 2 to 118, which is the exact count.

**One has lost the front of its name.** `+-s-contributions-to-developmental-psychology _ key_ building blocks, stages.png`. Canva has eaten the leading part of the slug, almost certainly a name with a character it would not carry. **Do not guess it.** Match it by its key words: `building blocks, stages` appears once in the map's `magic_media_key_words` column, and that row's `slug` is the correct name. Rename it to that slug and say in your report which row it was, so Chat can note whether the map row itself needs correcting.

## 4. What to do

1. Rename all 118 by the rule in section 2, discarding the row 1 file and resolving the one broken name by section 3.
2. Move the renamed masters into `Page Images` itself, where the S340 brief expects them, and leave the Canva subfolder and the zip where they are for now. **Do not delete either**; Kain deletes his own originals.
3. Check the 117 names against `ARTICLE_HERO_IMAGE_MAP_S340.csv` rows 2 to 118 in both directions: every map row has a master, every master has a map row. **Report any row without a picture and any picture without a row rather than proceeding past it.** Row 118 is `unlock-personal-empowerment-with-the-empowerment-dynamic`, added by Chat this session; its record is now gate-clean and importable (Cowork's `NOTE__Row_110_Ruling_Confirmed_Record_Finalized_S342`).
4. Run steps 1 to 5 of the S340 brief over all 117: the pipeline at the hero slot, 680 and 1360, the 2x as the attachment original, alt text from each record, the media gate on the rendered page.
5. Re-attach on the 83 drafts that already carry a picture, because Kain's revisions mean some of those pictures are now out of date, and attach on the rest as their records import.

## 5. What this unblocks, and what it does not

It clears the last of Kain's side of the salvage image work: **no rescued article is now waiting on a picture.** What still gates publishing is unchanged and is Kain's: his Safari look at a sample of the drafts, then his word. The nine that waited on Cowork's field fix are already in as drafts (your own S102 report), so the pile he looks at is 83 plus whatever imports before the sitting.

OWED BACK: the rename table, the two-way check against the map with any gap named, the pipeline printout, and the map row behind the broken filename.

*No em or en dashes in this file; checked before writing.*
