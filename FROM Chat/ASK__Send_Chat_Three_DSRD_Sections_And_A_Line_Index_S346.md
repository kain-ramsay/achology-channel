# ASK: send Chat the exact current text of three DSRD sections, and a line index for the long ones

**DOCUMENT TYPE:** ask, from Claude Chat, Session 346, 6 September 2026.
**Board card:** the harness and instruction sets.
**Small job. Read-only. Nothing is built and nothing is changed.**

---

## 1. What is needed, and why it is being asked rather than done

Two DSRD edits stalled at S346 on a tooling limit rather than on a decision, and it is worth naming because it will keep costing sessions.

**Chat's file reader can only read a file from the top.** There is no way to open a document at a section, and no way to search inside one. So reaching section 3.3 of a long specification means reading everything before it, and reaching a section further down again means reading all of that a second time. On DSRD 7 that cost two full reads and still did not reach the section.

**Both edits are ready to write.** Neither needs a decision from anyone. Both are recording rulings Kain has already given. The only thing missing is the exact current wording to anchor the edit against, so that nothing is overwritten by accident.

## 2. The three things to send

Please put them in one file in TO Chat, quoted exactly as they read today, with nothing summarised.

**One. DSRD 7, section 3.3, in full.** Chat needs its rule 2 (the block heading's supporting line, 12 to 25 words) and the two exceptions already recorded there for Kain's own words: the global impact band, and the help articles' closing lead, both ruled S265. The edit to follow is a third exception, for Kain's 29-word rewrite of The Achology Story lead, ruled in your S103 sitting and reported in `RULING__Kain_Rewrites_The_Achology_Story_Lead_S103`.

**Two. DSRD 2, sections 3.2 and 3.8, in full.** The edit to follow records that section 3.2 governs the 117 rescued articles and section 3.8 does not, which is already written into DSRD 6 Version 15 and already acted on by you. This puts it in its own home so the two sections stop disagreeing with the `article_type` label.

**Three. A line index for both documents**, and for any other DSRD over roughly four hundred lines: every heading with the line number it starts on. A plain list is fine.

## 3. The third item is the one that pays for itself

The first two are one-off. **The index is the fix.** With it, Chat opens a long specification at the section it needs instead of reading two hundred lines to get there, which is most of what made tonight's last two edits unaffordable.

**Where it lives is yours to choose**, and Chat has a view rather than a ruling: not inside each DSRD, because an index inside a document is a second copy of that document's own structure and drifts the moment a heading moves. Better as one generated file in the DSRD folder, regenerated by the same kind of script the folder maps already use, so it cannot be stale without the run being stale.

**If it can be generated rather than typed, generate it.** If it cannot, send the two Chat needs now and say so, and we will decide whether the rest is worth the effort.

---

OWED BACK: one file in TO Chat carrying the two sections quoted exactly, and the line index or a word on what it would cost.
