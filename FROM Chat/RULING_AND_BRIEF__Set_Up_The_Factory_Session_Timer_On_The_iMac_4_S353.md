> **CODE DISPOSITION, S107: WAITS ON Kain at the keyboard, which is this file's own instruction and not a delay Code can remove.** Arrived late in the sitting and read in full the moment H6 stopped the next edit. Section 2 step 7 says the setup is done with Kain beside Code, answering every permission prompt himself and watching run zero to its close, so it cannot be done in a sitting he is not sitting in, and doing the earlier steps alone would leave a task configured but unproved. Nothing in it is questioned or blocked otherwise: the local route rather than cloud is right for the reasons given, and section 5's check that a scheduled session sees the same FROM Chat and the same hooks is the right thing to prove before trusting it. **It is the first item on Code's next sitting with Kain.** Testable when a routine named `factory-session` exists on the iMac 4 with its run zero reported.

# RULING AND BRIEF: set up the factory session timer on the iMac 4, step one of the automation plan

**From:** Claude Chat, Session 353. **Date:** Tuesday 8 September 2026.
**Read cold.** Standalone. The plan it carries out is `PROPOSAL__The_Three_Claudes_On_A_Clock_Kain_Approves_Only_S353.md` at the root of the Project Delivery System folder; you do not need it to act, but read it once so the shape is in your head.
**Board card:** Automation plan: the three Claudes run on a clock, Kain approves only.

---

## 1. Kain's ruling, S353

Step one of the automation plan is approved: **Code runs a factory session on a timer, on the iMac 4, weekdays at 08:00, with nobody typing.** A three-run trial, graded by Chat at its opens from your session reports. Steps two (Cowork on a clock) and three (Code reads Cowork's import-ready DONE files directly) are not approved yet and nothing here touches them.

The facts this rests on were read from Anthropic's documentation today: Claude Code Desktop's Routines page offers a **Local** task (a saved prompt, a folder, a permission mode, a schedule) that starts a fresh session on the timer, runs the project's hooks and the allow rules in `~/.claude/settings.json`, fires only while the app is open and the Mac awake, and catches up one missed run on wake. Cloud routines are refused for this project because they run on a fresh clone away from your machine, the channel, the install key and the hooks.

## 2. What you set up, in your next sitting, with Kain beside you

1. In Claude Code Desktop on the iMac 4, Code tab, **Routines**, **New routine**, **Local**.
2. Name: `factory-session`. Description: "Achology factory session on a timer: works FROM Chat, never deploys, never publishes."
3. Folder: the folder you open every factory session from today. Permission mode: the mode your factory sessions run in today, tightened if anything, never loosened; the harness hooks run inside the scheduled session exactly as in a typed one. Model: the tier the session type uses today (opusplan, per the ai-collaboration skill), unless the picker offers no such choice, in which case name what you chose in your report.
4. Schedule: **Weekdays, 08:00.**
5. Instructions: the text in section 3 below, pasted whole.
6. Settings, Desktop app, General: **Keep computer awake** on, so the 08:00 run is not skipped.
7. Click **Run now** with Kain beside you. Answer every permission prompt with "always allow", once each, so no later run stalls. Watch the run to its close. That supervised run is run zero; the three trial runs are the next three weekday mornings.

Report the setup in your session report: the task name, folder, mode, model, the list of tools "always allowed", and what run zero did.

## 3. The task's instructions, pasted whole

> You are Claude Code running the Achology.com factory session on a timer, with nobody typing. Kain is not here and cannot be asked anything.
>
> Open exactly as The Harness Rule 1 says, as a factory session. Read every file in FROM Chat and name each one and its fate. If a run has started late because the Mac was asleep, do only what is still current.
>
> Do only factory work that a brief or ruling in FROM Chat already commissions: import records as drafts, run the gates, read Rank Math scores, run the DSRD 6 machine chapters, take measurements, correct records where a Chat ruling names the exact change, archive every FROM Chat file whose work is fully executed and verified, commit and push. Where a piece of work needs a spec, a ruling or a fact you do not have, stop that piece and write the question to TO Chat (Rule 5); never fill the gap with judgement.
>
> Never deploy the theme: this is a factory session and touches no theme file. Never publish, unpublish or take down any page: the publishing wall refuses it and you do not argue with the wall or ask for an override. Never write a DSRD, The Harness or The Shared Rules.
>
> If FROM Chat holds nothing to act on, write one line to TO Chat saying so and stop.
>
> Close as Rule 13 says: archive executed files, write `SESSION_REPORT__S{NNN}_TIMED.md` to TO Chat naming what ran, what was refused and why, what is owed back, and the tools this run used, then push the channel. Do not start work you cannot finish inside this run.

## 4. The trial, and how it is judged

Three unattended weekday runs. Chat reads each report at its next open and grades it on four things: the report exists; the open and close ran as the Harness says; nothing outside a FROM Chat commission was touched; no fence was tested. A run that fails any of the four pauses the task (Status: Paused on its detail page) and the fault is fixed in its home before a fourth run. Three clean runs and step two goes to Kain.

## 5. One thing to check before run zero

Confirm that the scheduled session sees the same `FROM Chat` folder and the same hooks a typed session does, by listing the folder and printing the hook acceptance line inside run zero. If either differs, stop and report rather than run the task on a different footing from your typed sessions.

OWED BACK: the setup report from your next sitting with the always-allowed tool list and run zero's account; then one TIMED report per weekday morning.

*No em or en dashes in this file; checked before writing.*
