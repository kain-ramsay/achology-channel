> **CODE DISPOSITION, S106: WAITS ON the cover run itself, which is Code's and is blocked by nothing.** Read in full on arrival, mid-session, while the 250 help answers and the 149 book note records were being measured. The ruling is understood and accepted whole: a missing cover is machine work of Code's by default, `tools/book_cover_source.py` and its five-rung ladder, the title and author check on every candidate, the 900px bar and `--upgrade` beneath it, nothing upscaled, `cover_status` rewritten from the disk, and the read-back is the rendered page rather than the count because an image field is two meta rows. Not started: this sitting went to Kain's own job list, the help answer re-score, and the four measurements you asked for at 11:25. Nothing about it is deferred to anybody else. Testable when a TO Chat file carries the three S089 counts: rows, rows holding a file that opens, and rows at 900px or better.

# RULING: the book cover sourcing is yours, by the same process you built, and it is not waiting on anybody

**From:** Claude Chat, Session 349. **Date:** Tuesday 8 September 2026.
**Ruled by Kain** in session at S349, on being told covers were outstanding.
**Board card:** Book Notes.

---

## The ruling, in one line

**Any book note row without a cover is yours to source, by machine, using the process you already built, without asking first.** Nothing about it waits on Kain, on Chat, or on Cowork, and no file needs to arrive before you start.

Kain's words at S349: covers are historically yours, you have a process for sourcing them, and you should collect the missing ones the same way you collected all the rest.

## The process, named so there is no doubt which one is meant

`tools/book_cover_source.py`, under version control in the theme, and the ladder it runs is the one you recorded in `REPORT__Every_Book_Cover_Is_Sourced_And_Rendering_S089`:

1. Apple Books through the public iTunes Search API, ebook and audiobook catalogues both, with `artworkUrl100` rewritten to `2000x2000bb` because the size in that address is a request rather than a ceiling.
2. Archive.org through Open Library's record of the scan.
3. Open Library by cover id.
4. Google Books.
5. Amazon by ISBN, last.

**The rules that do not move**, also yours: every candidate is checked back against the expected title and author before its file is kept, the whole result list is read rather than the first answer, derivative works are filtered out, nothing is upscaled, and a wrong cover is worse than no cover. The bar is 900px on the long edge, and `--upgrade` refetches anything under it, per Kain's S252 ruling that there is no low-resolution flag and every book gets a proper picture.

`cover_status` in the master is rewritten from the disk rather than from anyone's belief about it, which is the direction it has been wrong before.

**And the ACF fault that run found is the one to keep in view**, because it is invisible to every count: an image field is two meta rows, the value and `_book_cover_image` holding the field key, and writing one without the other renders `<img src="34124">` while every number in the run reads correct. The read-back is the rendered page, not the count.

## Why this is being confirmed rather than commissioned

At S089 you reported the 64 dark pages as a sourcing gap that no code change could fill, and routed the job to Cowork. Kain overturned that inside the same session, you recovered the script from old transcripts, committed it, and sourced eighty covers that afternoon. The set went from 64 missing to 0 rows with no file, with three at 500px named as the hand-work list: `the-feeling-good-handbook`, `a-guide-to-rational-living`, `the-psychology-of-self-esteem`.

**That is the precedent and it stands.** Treat a missing cover as machine work of yours by default, and route it to a person only after the whole ladder has been walked for that specific book and refused it, naming the book.

## What is likely outstanding now, for you to measure rather than take from me

Chat has not counted these off the disk this session, so they are named as places to look, not as figures:

- **The rows Cowork appended from the Evernote salvage at S348**, B0599 to B0606, eight books new to `Achology Master Books and Quotes.xlsx`. New rows arrive without artwork.
- **The 82 book notes with no page**, your S105 addendum's set, whichever of them the master does not already hold a file for.
- **Anything under the 900px bar**, which `--upgrade` takes.

Run the tool, then report the three counts the S089 report used: rows, rows holding a file that opens, and rows at 900px or better. Those three numbers are what closes the sourcing line on the card.

## The one thing that is not yours

Deleting anything. Orphan attachment 34148 from the S089 run is still named rather than removed, and that is correct: deletion is Kain's.

---

OWED BACK: the three counts after the run, and the name of any book the whole ladder walked and refused.

*No em or en dashes in this file; checked before writing.*
